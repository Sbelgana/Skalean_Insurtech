# META-PROMPT PHASE B SPRINT 7.5b v3.0 -- Specifications Foundation Experts + PartsHub + Tow + Carrier
# Phase B = taches detaillees pour Cowork avec code patterns + schemas SQL + types TS complets
# AUCUNE EMOJI AUTORISEE

**Version** : v3.0 (NOUVEAU sprint v3.0 -- foundation extensions pour Sprints 14/21/22.5/22.7/24/26.5)
**Phase** : 2 -- Securite (extensions Sprint 7 RBAC)
**Sprint** : 7.5b / 40 (cumul v3.0) -- Sprint inter-phases
**Reference orchestrateur** : `C-7.5b-sprint-7.5b-specifications-v3.md`
**Reference verification** : `V-7.5b-sprint-7.5b-verification.md`
**Numerotation taches** : 2.5.1 a 2.5.10
**Effort total** : ~10 heures developpement / 2 jours
**Apport metier** : Foundation packages + types + permissions + cross-tenant pour 6 acteurs ecosystem

---

## POSITION DANS LA PHASE

Sprint 7.5b (2.5) suit Sprint 7.5a (Decision Pack + roles RBAC 26) et precede Sprint 8 (CRM + Booking).

**Sprints consommateurs** (downstream) :
- Sprint 14 : utilise @insurtech/expertise package + 3 entites preview + permissions
- Sprint 21 : utilise @insurtech/expertise extensions + PartsHub permissions
- Sprint 22.5 : utilise @insurtech/tow package + 5 cross-tenant types
- Sprint 22.7 : utilise @insurtech/expertise full package
- Sprint 24 : utilise tous packages + tous cross-tenant types
- Sprint 26.5 : utilise packages carrier + replacement mocks

---

## DEPENDANCES

**Entrees consommees** :
- Sprint 7 RBAC 12 roles -> etendu Sprint 7.5a a 26 roles (acteur Tow + Expert + Carrier + PartsHub + Customer differentie)
- Sprint 6 multi-tenant (cross_tenant_authorizations table) -> etendu 7 types
- Sprint 5 auth foundation (JWT + tenants)
- Decisions 011-015 (rebrand + 6 acteurs + expert central + PartsHub + Demo Day)

**Sorties produites** (consumed downstream) :
- `@insurtech/expertise` package (types + interfaces + service squelettes)
- `@insurtech/tow` package (types + interfaces + service squelettes)
- 7 cross-tenant types (vs 3 v2.2)
- 130 permissions (vs 85 v2.2) -- 10 expertise + 7 parts + 5 tow + 6 carrier + 12 customer + 90 v2.2 conservees
- 3 entites foundation (insure_experts + insure_expert_assignments + insure_expert_reports squelettes)
- Documentation Sprint 22.7 Expert App extension path

---

## DECISIONS STRATEGIQUES APPLICABLES

- **decision-011-assurflow-rebrand** : naming Skalean (company) / Assurflow (vertical InsurTech) / Sofidemy (brand)
- **decision-012-6-acteurs-ecosystem** : Customer + Carrier + Garage + Tow + Expert + Broker
- **decision-013-expert-acteur-central** : workflow expert valide devis avant carrier paye
- **decision-014-partshub-phase-1** : module integre Assurflow Garage + commission 3-5%
- **decision-006-no-emoji** : 0 emoji absolu

---

## REGLES ABSOLUES skalean-insurtech v3.0

(Identique B-07/B-14/B-21/B-24 -- multi-tenant + Zod + Pino + Kafka + RBAC + Tests Vitest + TypeScript strict + pnpm + @insurtech/* + AUCUNE EMOJI + Idempotency-Key + Conventional Commits)

**Specifique Sprint 7.5b v3.0** :
- **Package structure** : `@insurtech/expertise` + `@insurtech/tow` suivent meme pattern que `@insurtech/repair` ou `@insurtech/insure`
- **Types TypeScript strict** : exports + interfaces + types unions + Zod schemas
- **Service squelettes** : signatures completes mais implementation defere Sprint 14+
- **Permissions enum** : convention `{module}.{resource}.{action}` strictement respectee
- **Cross-tenant** : 4 nouveaux types ajoutes au CHECK constraint cross_tenant_authorizations.type
- **Migrations** : numerotation Sprint07b- prefix pour traceability
- **RLS + FORCE** : sur les 3 nouvelles tables foundation

---

## EXECUTION SEQUENTIELLE DES 10 TACHES

---

### Tache 2.5.1 : Package @insurtech/expertise structure + types

**Metadonnees** : P0 | 1h | Depend de : Sprint 7.5a

**But** : Creer package `@insurtech/expertise` dans monorepo avec structure standard + types TypeScript exhaustifs pour Expert + ExpertAssignment + ExpertReport + 4 roles experts.

**Actions principales** :

Creer dossier `repo/packages/expertise/` avec structure :
```
packages/expertise/
  package.json
  tsconfig.json
  src/
    index.ts                    # Barrel export
    types/
      expert.types.ts
      expert-assignment.types.ts
      expert-report.types.ts
      expert-roles.types.ts
    schemas/
      expert.schema.ts          # Zod schemas
      expert-assignment.schema.ts
      expert-report.schema.ts
    services/
      experts-catalog.service.ts        # Squelette signatures only
      expert-assignments.service.ts     # Squelette
      expert-validation.service.ts      # Squelette (full Sprint 22.7)
      expert-reports-basic.service.ts   # Squelette (full Sprint 22.7)
    entities/
      insure-expert.entity.ts           # TypeORM entity squelette
      insure-expert-assignment.entity.ts
      insure-expert-report.entity.ts
```

**Pattern code `expert.types.ts`** :
```typescript
import { z } from 'zod';

export const ExpertTypeEnum = z.enum([
  'independent',
  'firm_admin',
  'associate',
  'carrier_internal'
]);

export type ExpertType = z.infer<typeof ExpertTypeEnum>;

export const ExpertStatusEnum = z.enum([
  'active',
  'pending_kyb',
  'suspended',
  'expired_agrement',
  'inactive'
]);

export type ExpertStatus = z.infer<typeof ExpertStatusEnum>;

export const ExpertSpecialtyEnum = z.enum([
  'auto_collision',
  'auto_mechanical',
  'auto_electrical',
  'auto_bodywork',
  'auto_total_loss',
  'auto_glass',
  'auto_marine',
  'auto_motorcycle',
  'auto_truck',
  'auto_fraud_investigation'
]);

export type ExpertSpecialty = z.infer<typeof ExpertSpecialtyEnum>;

export interface Expert {
  id: string;
  tenantId: string;
  userId: string;
  fullName: string;
  cinNumber: string;
  cinDocumentUrl: string;
  phone: string;
  email: string;
  acapsAgrementNumber: string;
  acapsAgrementDocumentUrl: string;
  acapsAgrementExpiryDate: Date;
  acapsSpecialty: ExpertSpecialty[];
  firmName: string | null;
  firmIce: string | null;
  expertType: ExpertType;
  carrierTenantId: string | null;
  activeZones: string[];
  totalMissions: number;
  avgRating: number;
  avgResponseTimeHours: number;
  baselineHonoraireMad: string;
  status: ExpertStatus;
  kybReviewedAt: Date | null;
  kybReviewedByUserId: string | null;
  kybRejectionReason: string | null;
  notes: string | null;
  createdAt: Date;
  updatedAt: Date;
}
```

**Pattern code `expert.schema.ts`** :
```typescript
import { z } from 'zod';
import { ExpertTypeEnum, ExpertSpecialtyEnum } from '../types/expert.types';

export const OnboardExpertSchema = z.object({
  fullName: z.string().min(3).max(255),
  cinNumber: z.string().regex(/^[A-Z]{1,2}\d{6,7}$/, 'Format CIN invalide'),
  cinDocumentUrl: z.string().url(),
  phone: z.string().regex(/^\+212[0-9]{9}$/, 'Format phone MA invalide'),
  email: z.string().email(),
  acapsAgrementNumber: z.string().regex(/^ACAPS-\d{4}-EXP-\d{3,5}$/),
  acapsAgrementDocumentUrl: z.string().url(),
  acapsAgrementExpiryDate: z.coerce.date().refine(d => d > new Date(), 'Agrement expire'),
  acapsSpecialty: z.array(ExpertSpecialtyEnum).min(1),
  expertType: ExpertTypeEnum,
  firmName: z.string().optional(),
  firmIce: z.string().regex(/^\d{15}$/).optional(),
  carrierTenantId: z.string().uuid().optional(),
  activeZones: z.array(z.string()).min(1),
  baselineHonoraireMad: z.string().regex(/^\d+(\.\d{1,2})?$/)
});

export type OnboardExpertInput = z.infer<typeof OnboardExpertSchema>;
```

**Validation** :
```bash
cd repo/packages/expertise
pnpm tsc --noEmit
pnpm vitest run
cd ../../..
```

**Commit** :
```bash
git add -A
git commit -m "feat(sprint-7.5b): package @insurtech/expertise structure + types

Task: 2.5.1
Sprint: 7.5b (Phase 2 / Sprint 5)
Phase: 2 -- Securite extensions
Decisions: decision-012 + decision-013"
```

---

### Tache 2.5.2 : Package @insurtech/tow structure + types

**Metadonnees** : P0 | 1h | Depend de : 2.5.1

**But** : Creer package `@insurtech/tow` avec types pour TowOperator + TowMission + TowMissionLocation + 7 etats mission workflow.

**Actions principales** :

Structure identique `@insurtech/expertise`. Patterns code `tow.types.ts` :

```typescript
import { z } from 'zod';

export const TowMissionStatusEnum = z.enum([
  'designated',
  'accepted',
  'en_route',
  'on_site',
  'towing',
  'delivered',
  'closed',
  'rejected',
  'cancelled'
]);

export type TowMissionStatus = z.infer<typeof TowMissionStatusEnum>;

export const TruckTypeEnum = z.enum(['light', 'medium', 'heavy']);
export type TruckType = z.infer<typeof TruckTypeEnum>;

export interface TowOperator {
  id: string;
  tenantId: string;
  ownerUserId: string;
  fullName: string;
  cinDocumentUrl: string;
  truckPlate: string;
  truckRegistrationUrl: string;
  towPermitUrl: string;
  iceNumber: string;
  insuranceDocUrl: string;
  zonesActive: string[];
  truckCapacityKg: number;
  truckType: TruckType;
  status: 'active' | 'pending_kyb' | 'suspended' | 'inactive';
  totalMissions: number;
  avgRating: number;
  baselineHonoraireMad: string;
  createdAt: Date;
  updatedAt: Date;
}

export interface TowMission {
  id: string;
  tenantId: string;
  carrierTenantId: string;
  towTenantId: string;
  towOperatorId: string;
  sinistreId: string;
  customerUserId: string;
  pickupLat: number;
  pickupLng: number;
  pickupAddress: string;
  destinationGarageId: string;
  destinationLat: number;
  destinationLng: number;
  truckTypeRequired: TruckType;
  estimatedDistanceKm: number;
  estimatedDurationMin: number;
  honoraireBaseMad: string;
  honoraireExtrasMad: string;
  honoraireTotalMad: string;
  status: TowMissionStatus;
  designatedAt: Date;
  acceptedAt: Date | null;
  rejectedAt: Date | null;
  rejectionReason: string | null;
  enRouteAt: Date | null;
  onSiteAt: Date | null;
  towingStartedAt: Date | null;
  deliveredAt: Date | null;
  closedAt: Date | null;
  cancelledAt: Date | null;
  cancelledReason: string | null;
  notes: string | null;
  createdAt: Date;
  updatedAt: Date;
}
```

**Commit** :
```bash
git commit -m "feat(sprint-7.5b): package @insurtech/tow structure + types

Task: 2.5.2
Sprint: 7.5b (Phase 2 / Sprint 5)
Decisions: decision-012"
```

---

### Tache 2.5.3 : Permissions enum extensions Sprint 7 (130 perms total)

**Metadonnees** : P0 | 1h | Depend de : 2.5.2

**But** : Etendre `permissions.enum.ts` Sprint 7 avec 45 nouvelles permissions v3.0 (10 expertise.* + 7 parts.* + 5 tow.* + 6 carrier.* + 17 customer.*) -> 130 perms total.

**Actions principales** :

Update `repo/packages/auth/src/rbac/permissions.enum.ts` :

```typescript
export enum Permission {
  // ... 85 permissions v2.2 conservees ...

  // === Sprint 7.5b v3.0 EXTENSIONS ===

  // Expertise (10 perms)
  EXPERTISE_ASSIGNMENTS_READ = 'expertise.assignments.read',
  EXPERTISE_ASSIGNMENTS_ACCEPT = 'expertise.assignments.accept',
  EXPERTISE_ASSIGNMENTS_REJECT = 'expertise.assignments.reject',
  EXPERTISE_VISIT_SCHEDULE = 'expertise.visit.schedule',
  EXPERTISE_VISIT_RECORD = 'expertise.visit.record',
  EXPERTISE_DEVIS_VALIDATE = 'expertise.devis.validate',
  EXPERTISE_DEVIS_MODIFY = 'expertise.devis.modify',
  EXPERTISE_DEVIS_REJECT = 'expertise.devis.reject',
  EXPERTISE_REPORT_CREATE = 'expertise.report.create',
  EXPERTISE_REPORT_SIGN = 'expertise.report.sign',
  EXPERTISE_REPORT_SUBMIT = 'expertise.report.submit',
  EXPERTISE_REPORT_READ = 'expertise.report.read',

  // PartsHub (7 perms)
  PARTS_SUPPLIERS_READ = 'parts.suppliers.read',
  PARTS_SUPPLIERS_ADD_TO_FAVORITES = 'parts.suppliers.add_to_favorites',
  PARTS_ORDERS_CREATE = 'parts.orders.create',
  PARTS_ORDERS_READ = 'parts.orders.read',
  PARTS_ORDERS_CANCEL = 'parts.orders.cancel',
  PARTS_PAYMENTS_INITIATE = 'parts.payments.initiate',
  PARTS_COMMISSION_READ = 'parts.commission.read',

  // Tow (5 perms)
  TOW_MISSIONS_READ = 'tow.missions.read',
  TOW_MISSIONS_ACCEPT = 'tow.missions.accept',
  TOW_MISSIONS_REJECT = 'tow.missions.reject',
  TOW_MISSIONS_UPDATE_STATUS = 'tow.missions.update_status',
  TOW_HONORAIRES_READ = 'tow.honoraires.read',

  // Carrier (6 perms additionnelles vs Sprint 7)
  CARRIER_EXPERTS_DESIGNATE = 'carrier.experts.designate',
  CARRIER_EXPERTS_READ_POOL = 'carrier.experts.read_pool',
  CARRIER_TOW_DESIGNATE = 'carrier.tow.designate',
  CARRIER_EXPERT_REPORTS_REVIEW = 'carrier.expert_reports.review',
  CARRIER_EXPERT_REPORTS_ACCEPT = 'carrier.expert_reports.accept',
  CARRIER_EXPERT_REPORTS_CONTEST = 'carrier.expert_reports.contest',

  // Customer (17 perms differenciee acteur 5)
  CUSTOMER_SINISTRES_DECLARE_FNOL = 'customer.sinistres.declare_fnol',
  CUSTOMER_SINISTRES_READ_MINE = 'customer.sinistres.read_mine',
  CUSTOMER_SINISTRES_TRACK_PROGRESS = 'customer.sinistres.track_progress',
  CUSTOMER_DEVIS_VIEW = 'customer.devis.view',
  CUSTOMER_DEVIS_ACCEPT = 'customer.devis.accept',
  CUSTOMER_REPAIRS_READ_MINE = 'customer.repairs.read_mine',
  CUSTOMER_DELIVERY_SIGN = 'customer.delivery.sign',
  CUSTOMER_TOW_TRACK = 'customer.tow.track',
  CUSTOMER_POLICIES_VIEW = 'customer.policies.view',
  CUSTOMER_POLICIES_RENEW = 'customer.policies.renew',
  CUSTOMER_PAYMENTS_VIEW = 'customer.payments.view',
  CUSTOMER_PAYMENTS_INITIATE = 'customer.payments.initiate',
  CUSTOMER_DOCUMENTS_READ = 'customer.documents.read',
  CUSTOMER_NOTIFICATIONS_MANAGE = 'customer.notifications.manage',
  CUSTOMER_FEEDBACK_SUBMIT = 'customer.feedback.submit',
  CUSTOMER_SUPPORT_CONTACT = 'customer.support.contact',
  CUSTOMER_PROFILE_UPDATE = 'customer.profile.update'
}
```

**Update permissions-matrix.ts** : associer 4 roles experts + 5 roles tow + 6 roles carrier + customer aux nouvelles perms.

**Tests update permissions-matrix.spec.ts** :
- Count >= 130 permissions
- expert_independent : `EXPERTISE_*` complete
- carrier_expert_manager : `CARRIER_EXPERTS_DESIGNATE` PASS
- garage_parts_manager : `PARTS_*` complete
- tow_chauffeur : `TOW_MISSIONS_*` (sauf reject = tow_admin only)
- customer : 17 `CUSTOMER_*` perms

**Validation** :
```bash
cd repo
pnpm tsc --noEmit
pnpm vitest run --coverage packages/auth/src/rbac
pnpm lint
cd ..
```

**Commit** :
```bash
git commit -m "feat(sprint-7.5b): permissions enum +45 perms = 130 total v3.0

Task: 2.5.3
Sprint: 7.5b (Phase 2 / Sprint 5)
Decisions: decision-012 + sprint 7.5a 26 roles"
```

---

### Tache 2.5.4 : Cross-tenant types extensions (7 types vs 3 v2.2)

**Metadonnees** : P0 | 1h | Depend de : 2.5.3

**But** : Etendre Sprint 6 `cross_tenant_authorizations.type` CHECK constraint avec 4 nouveaux types.

**Actions principales** :

Migration `repo/packages/database/src/migrations/{date}-Sprint7.5b-CrossTenantTypesExtensions.ts` :

```typescript
import { MigrationInterface, QueryRunner } from 'typeorm';

export class Sprint75bCrossTenantTypesExtensions{TIMESTAMP} implements MigrationInterface {
  name = 'Sprint75bCrossTenantTypesExtensions{TIMESTAMP}';

  public async up(queryRunner: QueryRunner): Promise<void> {
    // Drop existing constraint (3 types v2.2)
    await queryRunner.query(`
      ALTER TABLE cross_tenant_authorizations
      DROP CONSTRAINT IF EXISTS chk_cross_tenant_type;
    `);

    // Add new constraint with 7 types
    await queryRunner.query(`
      ALTER TABLE cross_tenant_authorizations
      ADD CONSTRAINT chk_cross_tenant_type
      CHECK (type IN (
        'carrier_to_garage_visibility',
        'broker_to_carrier_query',
        'garage_to_carrier_share',
        'carrier_to_tow_assignment',
        'carrier_to_expert_assignment',
        'garage_to_expert_request',
        'tow_to_carrier_status'
      ));
    `);

    // Add indexes for new types
    await queryRunner.query(`
      CREATE INDEX idx_cross_tenant_type_carrier_tow ON cross_tenant_authorizations(type)
      WHERE type = 'carrier_to_tow_assignment';

      CREATE INDEX idx_cross_tenant_type_carrier_expert ON cross_tenant_authorizations(type)
      WHERE type = 'carrier_to_expert_assignment';

      CREATE INDEX idx_cross_tenant_type_garage_expert ON cross_tenant_authorizations(type)
      WHERE type = 'garage_to_expert_request';

      CREATE INDEX idx_cross_tenant_type_tow_carrier ON cross_tenant_authorizations(type)
      WHERE type = 'tow_to_carrier_status';
    `);
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`DROP INDEX IF EXISTS idx_cross_tenant_type_tow_carrier;`);
    await queryRunner.query(`DROP INDEX IF EXISTS idx_cross_tenant_type_garage_expert;`);
    await queryRunner.query(`DROP INDEX IF EXISTS idx_cross_tenant_type_carrier_expert;`);
    await queryRunner.query(`DROP INDEX IF EXISTS idx_cross_tenant_type_carrier_tow;`);

    await queryRunner.query(`
      ALTER TABLE cross_tenant_authorizations
      DROP CONSTRAINT chk_cross_tenant_type;
    `);

    await queryRunner.query(`
      ALTER TABLE cross_tenant_authorizations
      ADD CONSTRAINT chk_cross_tenant_type
      CHECK (type IN (
        'carrier_to_garage_visibility',
        'broker_to_carrier_query',
        'garage_to_carrier_share'
      ));
    `);
  }
}
```

Update TypeScript types :
```typescript
// repo/packages/auth/src/types/cross-tenant.types.ts
export const CrossTenantTypeEnum = z.enum([
  'carrier_to_garage_visibility',
  'broker_to_carrier_query',
  'garage_to_carrier_share',
  'carrier_to_tow_assignment',
  'carrier_to_expert_assignment',
  'garage_to_expert_request',
  'tow_to_carrier_status'
]);

export type CrossTenantType = z.infer<typeof CrossTenantTypeEnum>;
```

Update service `cross-tenant.service.ts` : whitelist new types + permissions check par type.

**Tests 8+** :
- Migration up/down OK
- 4 new types acceptes
- Constraint reject autres types
- Indexes partial fonctionnels
- TypeORM Entity sync

**Commit** :
```bash
git commit -m "feat(sprint-7.5b): cross-tenant types extensions 3 -> 7 types

Task: 2.5.4
Sprint: 7.5b (Phase 2 / Sprint 5)
Decisions: decision-012 ecosystem 6 acteurs"
```

---

### Tache 2.5.5 : Foundation entity insure_experts (squelette)

**Metadonnees** : P0 | 1h | Depend de : 2.5.4

**But** : Migration squelette `insure_experts` (full implementation Sprint 14 Tache 4.1.15).

**Actions principales** :

Migration squelette `repo/packages/database/src/migrations/{date}-Sprint7.5b-InsureExpertsSkeleton.ts` :

```typescript
public async up(queryRunner: QueryRunner): Promise<void> {
  await queryRunner.query(`
    CREATE TABLE insure_experts (
      id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
      tenant_id UUID NOT NULL REFERENCES tenants(id),
      user_id UUID NOT NULL REFERENCES users(id),
      full_name VARCHAR(255) NOT NULL,
      cin_number VARCHAR(20) NOT NULL,
      cin_document_url TEXT NOT NULL,
      phone VARCHAR(20) NOT NULL,
      email VARCHAR(255) NOT NULL,
      acaps_agrement_number VARCHAR(50) NOT NULL UNIQUE,
      acaps_agrement_document_url TEXT NOT NULL,
      acaps_agrement_expiry_date DATE NOT NULL,
      acaps_specialty TEXT[] NOT NULL DEFAULT '{}',
      firm_name VARCHAR(255),
      firm_ice VARCHAR(15),
      expert_type VARCHAR(50) NOT NULL CHECK (expert_type IN ('independent', 'firm_admin', 'associate', 'carrier_internal')),
      carrier_tenant_id UUID REFERENCES tenants(id),
      active_zones TEXT[] NOT NULL DEFAULT '{}',
      total_missions INTEGER NOT NULL DEFAULT 0,
      avg_rating NUMERIC(3,2) NOT NULL DEFAULT 0,
      avg_response_time_hours NUMERIC(8,2) NOT NULL DEFAULT 0,
      baseline_honoraire_mad NUMERIC(12,2) NOT NULL DEFAULT 0,
      status VARCHAR(50) NOT NULL DEFAULT 'pending_kyb' CHECK (status IN ('active', 'pending_kyb', 'suspended', 'expired_agrement', 'inactive')),
      kyb_reviewed_at TIMESTAMP,
      kyb_reviewed_by_user_id UUID REFERENCES users(id),
      kyb_rejection_reason TEXT,
      notes TEXT,
      created_at TIMESTAMP NOT NULL DEFAULT NOW(),
      updated_at TIMESTAMP NOT NULL DEFAULT NOW(),

      CONSTRAINT chk_carrier_internal_tenant CHECK (
        (expert_type = 'carrier_internal' AND carrier_tenant_id IS NOT NULL) OR
        (expert_type != 'carrier_internal')
      )
    );

    CREATE INDEX idx_insure_experts_tenant ON insure_experts(tenant_id);
    CREATE INDEX idx_insure_experts_status ON insure_experts(status);
    CREATE INDEX idx_insure_experts_specialty ON insure_experts USING GIN(acaps_specialty);
    CREATE INDEX idx_insure_experts_zones ON insure_experts USING GIN(active_zones);
    CREATE INDEX idx_insure_experts_carrier_internal ON insure_experts(carrier_tenant_id)
      WHERE expert_type = 'carrier_internal';

    ALTER TABLE insure_experts ENABLE ROW LEVEL SECURITY;
    ALTER TABLE insure_experts FORCE ROW LEVEL SECURITY;

    CREATE POLICY insure_experts_tenant_isolation ON insure_experts
      USING (app_can_access_tenant(tenant_id));
  `);
}

public async down(queryRunner: QueryRunner): Promise<void> {
  await queryRunner.query(`DROP TABLE IF EXISTS insure_experts;`);
}
```

Entity squelette `repo/packages/expertise/src/entities/insure-expert.entity.ts` (signature only, full Sprint 14).

**Commit** :
```bash
git commit -m "feat(sprint-7.5b): foundation entity insure_experts squelette + rls

Task: 2.5.5
Sprint: 7.5b (Phase 2 / Sprint 5)
Decisions: decision-013 + preview sprint 14"
```

---

### Tache 2.5.6 : Foundation entity insure_expert_assignments (squelette)

**Metadonnees** : P0 | 1h | Depend de : 2.5.5

**But** : Migration squelette `insure_expert_assignments` (full implementation Sprint 14 Tache 4.1.16).

**Actions principales** :

```sql
CREATE TABLE insure_expert_assignments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID NOT NULL REFERENCES tenants(id),
  carrier_tenant_id UUID NOT NULL REFERENCES tenants(id),
  carrier_user_id UUID NOT NULL REFERENCES users(id),
  expert_tenant_id UUID NOT NULL REFERENCES tenants(id),
  expert_id UUID NOT NULL REFERENCES insure_experts(id),
  expert_user_id UUID NOT NULL REFERENCES users(id),
  sinistre_id UUID NOT NULL,
  garage_tenant_id UUID REFERENCES tenants(id),
  garage_address TEXT,
  garage_lat NUMERIC(10,7),
  garage_lng NUMERIC(10,7),
  status VARCHAR(50) NOT NULL DEFAULT 'designated' CHECK (status IN (
    'designated', 'accepted', 'rejected', 'in_progress', 'completed', 'cancelled'
  )),
  designated_at TIMESTAMP NOT NULL DEFAULT NOW(),
  accepted_at TIMESTAMP,
  rejected_at TIMESTAMP,
  rejection_reason TEXT,
  visit_scheduled_at TIMESTAMP,
  visit_completed_at TIMESTAMP,
  report_submitted_at TIMESTAMP,
  completed_at TIMESTAMP,
  cancelled_at TIMESTAMP,
  cancelled_reason TEXT,
  honoraire_mad NUMERIC(12,2),
  honoraire_invoice_id UUID,
  honoraire_payment_status VARCHAR(20) DEFAULT 'pending' CHECK (honoraire_payment_status IN (
    'pending', 'invoiced', 'paid', 'overdue'
  )),
  notes TEXT,
  created_at TIMESTAMP NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_insure_expert_assignments_tenant ON insure_expert_assignments(tenant_id);
CREATE INDEX idx_insure_expert_assignments_carrier ON insure_expert_assignments(carrier_tenant_id);
CREATE INDEX idx_insure_expert_assignments_expert ON insure_expert_assignments(expert_id);
CREATE INDEX idx_insure_expert_assignments_sinistre ON insure_expert_assignments(sinistre_id);
CREATE INDEX idx_insure_expert_assignments_status ON insure_expert_assignments(status);

ALTER TABLE insure_expert_assignments ENABLE ROW LEVEL SECURITY;
ALTER TABLE insure_expert_assignments FORCE ROW LEVEL SECURITY;

CREATE POLICY insure_expert_assignments_tenant_isolation ON insure_expert_assignments
  USING (app_can_access_tenant(tenant_id));
```

**Commit** :
```bash
git commit -m "feat(sprint-7.5b): foundation entity insure_expert_assignments squelette

Task: 2.5.6
Sprint: 7.5b (Phase 2 / Sprint 5)
Decisions: preview sprint 14 + decision-013"
```

---

### Tache 2.5.7 : Foundation entity insure_expert_reports (squelette)

**Metadonnees** : P0 | 1h | Depend de : 2.5.6

**But** : Migration squelette `insure_expert_reports` (full Sprint 14 Tache 4.1.17 + extension Sprint 22.7).

**Actions principales** :

```sql
CREATE TABLE insure_expert_reports (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID NOT NULL REFERENCES tenants(id),
  assignment_id UUID NOT NULL REFERENCES insure_expert_assignments(id),
  expert_id UUID NOT NULL REFERENCES insure_experts(id),
  expert_user_id UUID NOT NULL REFERENCES users(id),
  devis_id UUID,
  report_content JSONB NOT NULL DEFAULT '{}',
  photos_urls TEXT[] DEFAULT '{}',
  decision VARCHAR(20) CHECK (decision IN ('validated', 'modified', 'rejected')),
  decision_justification TEXT,
  modifications JSONB DEFAULT '{}',
  pdf_url TEXT,
  pdf_generated_at TIMESTAMP,
  signature_id UUID,
  signed_at TIMESTAMP,
  signature_legal_status VARCHAR(20) DEFAULT 'pending' CHECK (signature_legal_status IN (
    'pending', 'signed', 'expired'
  )),
  status VARCHAR(30) NOT NULL DEFAULT 'draft' CHECK (status IN (
    'draft', 'completed', 'signed', 'submitted_to_carrier', 'accepted_by_carrier', 'contested_by_carrier'
  )),
  submitted_to_carrier_at TIMESTAMP,
  carrier_received_at TIMESTAMP,
  notes TEXT,
  created_at TIMESTAMP NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_insure_expert_reports_tenant ON insure_expert_reports(tenant_id);
CREATE INDEX idx_insure_expert_reports_assignment ON insure_expert_reports(assignment_id);
CREATE INDEX idx_insure_expert_reports_expert ON insure_expert_reports(expert_id);
CREATE INDEX idx_insure_expert_reports_devis ON insure_expert_reports(devis_id);
CREATE INDEX idx_insure_expert_reports_status ON insure_expert_reports(status);

ALTER TABLE insure_expert_reports ENABLE ROW LEVEL SECURITY;
ALTER TABLE insure_expert_reports FORCE ROW LEVEL SECURITY;

CREATE POLICY insure_expert_reports_tenant_isolation ON insure_expert_reports
  USING (app_can_access_tenant(tenant_id));
```

Documentation `repo/docs/expert-reports-sprint-22.7-extension-path.md` (~80 lignes) decrivant extension Sprint 22.7 :
- Service `expert-validation.service.ts` full version (devis line-by-line decimal.js)
- Service `expert-report-signing.service.ts` (Barid eSign)
- Service `expert-report-submission.service.ts` (transition status)

**Commit** :
```bash
git commit -m "feat(sprint-7.5b): foundation entity insure_expert_reports squelette + doc extension

Task: 2.5.7
Sprint: 7.5b (Phase 2 / Sprint 5)
Decisions: preview sprint 14 + sprint 22.7 extension path"
```

---

### Tache 2.5.8 : Service squelettes @insurtech/expertise (signatures)

**Metadonnees** : P0 | 1h | Depend de : 2.5.7

**But** : Services squelettes signatures completes pour `experts-catalog.service.ts` + `expert-assignments.service.ts` + `expert-validation.service.ts` + `expert-reports-basic.service.ts`. Implementation defere Sprint 14.

**Actions principales** :

```typescript
// repo/packages/expertise/src/services/experts-catalog.service.ts

import { Injectable, NotImplementedException } from '@nestjs/common';
import { PinoLogger } from 'nestjs-pino';
import type { OnboardExpertInput } from '../schemas/expert.schema';
import type { Expert } from '../types/expert.types';

@Injectable()
export class ExpertsCatalogService {
  constructor(private readonly logger: PinoLogger) {
    this.logger.setContext(ExpertsCatalogService.name);
  }

  async onboardExpert(input: OnboardExpertInput, currentUserId: string): Promise<Expert> {
    this.logger.info({ action: 'onboardExpert', expertEmail: input.email });
    throw new NotImplementedException('Sprint 14 Tache 4.1.15 -- implementation defere');
  }

  async approveKyb(expertId: string, reviewerUserId: string): Promise<Expert> {
    this.logger.info({ action: 'approveKyb', expertId, reviewerUserId });
    throw new NotImplementedException('Sprint 14 Tache 4.1.15');
  }

  async rejectKyb(expertId: string, reviewerUserId: string, reason: string): Promise<Expert> {
    this.logger.info({ action: 'rejectKyb', expertId, reviewerUserId });
    throw new NotImplementedException('Sprint 14 Tache 4.1.15');
  }

  async suspendExpert(expertId: string, reason: string): Promise<Expert> {
    throw new NotImplementedException('Sprint 14 Tache 4.1.15');
  }

  async checkAgrementExpiry(): Promise<void> {
    this.logger.info({ action: 'checkAgrementExpiry' });
    throw new NotImplementedException('Sprint 14 Tache 4.1.15 -- cron daily');
  }

  async searchExperts(filters: {
    specialty?: string[];
    zones?: string[];
    status?: string;
    carrierTenantId?: string;
  }): Promise<Expert[]> {
    throw new NotImplementedException('Sprint 14 Tache 4.1.15');
  }
}
```

Memes patterns pour 3 autres services squelettes.

Tests squelette `experts-catalog.service.spec.ts` :
```typescript
describe('ExpertsCatalogService', () => {
  it('should be defined', () => { expect(service).toBeDefined(); });
  it('all methods throw NotImplementedException (Sprint 14)', () => {
    expect(() => service.onboardExpert({} as any, 'user-1')).toThrow(NotImplementedException);
    // ... 5 autres
  });
});
```

**Commit** :
```bash
git commit -m "feat(sprint-7.5b): services squelettes @insurtech/expertise signatures

Task: 2.5.8
Sprint: 7.5b (Phase 2 / Sprint 5)
Decisions: defere sprint 14 + sprint 22.7"
```

---

### Tache 2.5.9 : Services squelettes @insurtech/tow (signatures)

**Metadonnees** : P0 | 1h | Depend de : 2.5.8

**But** : Services squelettes `tow-operators.service.ts` + `tow-missions.service.ts` + `tow-honoraires.service.ts`. Implementation defere Sprint 22.5.

**Actions principales** :

Pattern identique Tache 2.5.8 -- signatures completes + NotImplementedException + tests squelettes.

```typescript
// repo/packages/tow/src/services/tow-missions.service.ts

@Injectable()
export class TowMissionsService {
  async designateMission(input: DesignateMissionInput, carrierUserId: string): Promise<TowMission> {
    throw new NotImplementedException('Sprint 22.5 Tache 5.4.3');
  }

  async acceptMission(missionId: string, towUserId: string): Promise<TowMission> {
    throw new NotImplementedException('Sprint 22.5 Tache 5.4.6');
  }

  async rejectMission(missionId: string, towUserId: string, reason: string): Promise<TowMission> {
    throw new NotImplementedException('Sprint 22.5 Tache 5.4.6');
  }

  async transitionStatus(missionId: string, newStatus: TowMissionStatus, payload?: any): Promise<TowMission> {
    throw new NotImplementedException('Sprint 22.5 Tache 5.4.7-10');
  }

  async cancelMission(missionId: string, reason: string): Promise<TowMission> {
    throw new NotImplementedException('Sprint 22.5');
  }

  async listMyMissions(towUserId: string, filters: any): Promise<TowMission[]> {
    throw new NotImplementedException('Sprint 22.5 Tache 5.4.5');
  }
}
```

**Commit** :
```bash
git commit -m "feat(sprint-7.5b): services squelettes @insurtech/tow signatures

Task: 2.5.9
Sprint: 7.5b (Phase 2 / Sprint 5)
Decisions: defere sprint 22.5"
```

---

### Tache 2.5.10 : Documentation extension paths + tests integration

**Metadonnees** : P0 | 2h | Depend de : 2.5.9

**But** : Documentation complete extension paths Sprint 14 + 22.5 + 22.7 + 26.5 + tests integration cross-package.

**Actions principales** :

Documents a produire dans `repo/docs/architecture/` :
- `expertise-extension-path.md` (~150 lignes) -- detaille comment Sprint 14/22.7 etendent les squelettes
- `tow-extension-path.md` (~120 lignes) -- detaille Sprint 22.5 implementation
- `cross-tenant-7-types-architecture.md` (~200 lignes) -- 7 types + permissions + flows
- `permissions-130-catalog.md` (~180 lignes) -- catalog complet 130 perms par role

Tests integration cross-package `repo/test/integration/sprint-7.5b-foundation.spec.ts` :
```typescript
describe('Sprint 7.5b Foundation Integration', () => {
  it('@insurtech/expertise exports types correctly', async () => {
    const { ExpertTypeEnum, ExpertSpecialtyEnum } = await import('@insurtech/expertise');
    expect(ExpertTypeEnum.options).toHaveLength(4);
    expect(ExpertSpecialtyEnum.options).toHaveLength(10);
  });

  it('@insurtech/tow exports types correctly', async () => {
    const { TowMissionStatusEnum, TruckTypeEnum } = await import('@insurtech/tow');
    expect(TowMissionStatusEnum.options).toHaveLength(9);
    expect(TruckTypeEnum.options).toHaveLength(3);
  });

  it('Permission enum count = 130', () => {
    const { Permission } = require('@insurtech/auth');
    expect(Object.keys(Permission)).toHaveLength(130);
  });

  it('Cross-tenant types CHECK constraint accepts 7 types', async () => {
    const types = [
      'carrier_to_garage_visibility',
      'broker_to_carrier_query',
      'garage_to_carrier_share',
      'carrier_to_tow_assignment',
      'carrier_to_expert_assignment',
      'garage_to_expert_request',
      'tow_to_carrier_status'
    ];
    for (const type of types) {
      const result = await pgPool.query(
        `INSERT INTO cross_tenant_authorizations (...) VALUES (..., $1, ...) RETURNING id`,
        [type]
      );
      expect(result.rows[0].id).toBeDefined();
    }
  });

  it('3 foundation entities RLS + FORCE active', async () => {
    const tables = ['insure_experts', 'insure_expert_assignments', 'insure_expert_reports'];
    for (const table of tables) {
      const result = await pgPool.query(
        `SELECT relrowsecurity AND relforcerowsecurity AS rls FROM pg_class WHERE relname = $1`,
        [table]
      );
      expect(result.rows[0].rls).toBe(true);
    }
  });
});
```

**Tests minimum 12 scenarios**.

**Commit** :
```bash
git commit -m "docs(sprint-7.5b): documentation extension paths + tests integration cross-package

Task: 2.5.10
Sprint: 7.5b (Phase 2 / Sprint 5)
Decisions: foundation extensions cross-sprints"
```

---

## SYNTHESE -- Cloture Sprint 7.5b

```bash
# 10 commits Sprint 7.5b
git log --since="3 days ago" --pretty=format:"%s" -- repo/ | grep "Task: 2.5" | wc -l
# Attendu : 10

# 0 emoji
grep -rP "[\x{1F300}-\x{1F9FF}]" repo/packages/expertise repo/packages/tow --include="*.ts" --include="*.md" | wc -l
# Attendu : 0

# Lancer V-7.5b
cat skalean-insurtech/00-pilotage/meta-prompts/phase-V-verification/V-7.5b-sprint-7.5b-verification.md

# Si GO (>= 95%)
git tag -a "sprint-7.5b-complete-v3-foundation" -m "Sprint 7.5b v3.0 Foundation complete

- @insurtech/expertise package + types + service squelettes
- @insurtech/tow package + types + service squelettes
- Permissions 130 total (45 nouvelles v3.0)
- Cross-tenant 7 types (vs 3 v2.2)
- 3 foundation entities (insure_experts + assignments + reports)
- Documentation extension paths Sprints 14/22.5/22.7/26.5"

git push origin sprint-7.5b-complete-v3-foundation
```

---

## METRIQUES DE VALIDATION

| Metrique | Cible | Mesure |
|----------|-------|--------|
| 10 commits Sprint 7.5b | 10/10 | git log Task: 2.5.* |
| Permissions enum count | >= 130 | `Object.keys(Permission).length` |
| Cross-tenant types | 7 | Migration CHECK constraint |
| RLS + FORCE 3 tables | 3/3 | pg_class.relrowsecurity AND relforcerowsecurity |
| Tests integration | >= 12 | Vitest run |
| Coverage @insurtech/expertise | >= 85% | Vitest coverage |
| Coverage @insurtech/tow | >= 85% | Vitest coverage |
| 0 emoji | 0 | grep recursif |
| Documentation extension | 4 docs | architecture/*.md |
| TypeScript strict | 0 errors | pnpm tsc --noEmit |

---

## CONFORMITE InsurTech Maroc v3.0

- **Audit ACAPS** : pas d'audit dans Sprint 7.5b (squelettes seulement, full Sprint 14+)
- **Loi 09-08 CNDP** : 3 entites foundation respectent isolation tenant via RLS + FORCE
- **Loi 43-20** : signature reports defere Sprint 22.7 (Barid eSign)
- **Naming v3.0** : decision-011 strictement applique (Skalean/Assurflow/Sofidemy)
- **6 acteurs** : 26 roles Sprint 7.5a + 7 types cross-tenant + 130 perms = ecosystem complet

---

## NOTES IMPORTANTES POUR EXECUTION

1. **Sprint 7.5b est foundation seulement** -- pas d'implementation business logic
2. **NotImplementedException** dans tous services squelettes -- Sprint 14+ implementent
3. **3 entites preview** sont migrations completes (RLS + indexes + CHECK constraints) car downstream sprints en dependent
4. **45 permissions nouvelles** = ne PAS oublier permissions-matrix.ts update + tests count
5. **7 cross-tenant types** = migration down() doit revenir aux 3 v2.2 (pour rollback)
6. **Documentation extension paths** = critiques pour Cowork comprenne Sprint 14+ deferent

---

**Fin meta-prompt B-7.5b v3.0 -- Sprint 7.5b (2.5) Foundation Experts + PartsHub + Tow + Carrier.**

**Total taches** : 10 | **Effort** : ~10h | **Apport** : Foundation packages + types + permissions + cross-tenant ecosystem 6 acteurs
