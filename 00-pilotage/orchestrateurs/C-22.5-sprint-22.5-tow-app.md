# ORCHESTRATEUR SPRINT 22.5 v3.0 -- Phase 5 / Sprint 4 : Tow App (Depanneur Mobile)
# 15 taches sequentielles + verification finale
# AUCUNE EMOJI AUTORISEE

**Version** : v3.0 (NOUVEAU sprint v3.0 -- ecosystem 6 acteurs)
**Phase** : 5 -- Vertical Repair (Assurflow Ecosystem)
**Sprint** : 22.5 / 40 (cumul v3.0) -- NOUVEAU sprint v3.0 (n'existait pas v2.2)
**Reference meta-prompt** : `B-22.5-sprint-22.5-tow-app.md` v3.0
**Reference verification** : `V-22.5-sprint-22.5-verification.md`
**Numerotation taches** : 5.4.1 a 5.4.15
**Effort total** : ~75 heures developpement / 1.5 semaines
**Apport metier** : Application mobile Tow Operator (depanneur) -- 5eme acteur ecosystem 6 acteurs

---

Tu es **Claude Code (ou Cowork)**. Execute **TOUTES les 15 taches** du Sprint 22.5 v3.0 **UNE PAR UNE** dans l'ordre defini, puis lance V-22.5.

**STRATEGIE NOUVEAU v3.0** : Sprint 22.5 n'existait pas v2.2 (le v2.2 ne modelait que 3 acteurs Customer + Carrier + Garage). Le v3.0 ajoute Tow Operator comme acteur central pour sinistres avec vehicule immobilise -- preparation par Sprints 7.5a (roles `tow_admin`/`tow_chauffeur`) + Sprint 7.5b (specifications).

---

## OBJECTIF DU SPRINT 22.5 v3.0

Sprint 22.5 (5.4) -- Tow App (Depanneur Mobile). Voir B-22.5 v3.0 pour contexte detaille.

Implementer **Application mobile Tow Operator** (React Native iOS + Android) :
- Authentification + onboarding KYB tow operator
- Reception missions designees par carriers (Sprint 24 Tache 5.6.3)
- Accept/reject mission + workflow intervention (en_route -> on_site -> towing -> delivered)
- GPS tracking real-time + photos vehicule etat initial
- Paiement honoraires par carrier
- Dashboard performance + ratings

---

## STRUCTURE DES FICHIERS

```
skalean-insurtech/00-pilotage/prompts-taches/sprint-22.5-tow-app/
  task-5.4.1-prompt.md   # Bootstrap React Native + monorepo apps/web-tow-app
  task-5.4.2-prompt.md   # tow_operators entity + KYB workflow
  task-5.4.3-prompt.md   # tow_missions entity + workflow 7 etats
  task-5.4.4-prompt.md   # Authentification + onboarding mobile
  task-5.4.5-prompt.md   # Screen 1 : Missions list (designees + active + history)
  task-5.4.6-prompt.md   # Screen 2 : Mission details + accept/reject
  task-5.4.7-prompt.md   # Screen 3 : Intervention en_route + GPS tracking
  task-5.4.8-prompt.md   # Screen 4 : On-site + photos vehicule etat initial
  task-5.4.9-prompt.md   # Screen 5 : Towing + transit GPS tracking
  task-5.4.10-prompt.md  # Screen 6 : Delivered + signature garage reception
  task-5.4.11-prompt.md  # Screen 7 : Honoraires + paiement carrier mock
  task-5.4.12-prompt.md  # Screen 8 : Dashboard + KPIs personnel
  task-5.4.13-prompt.md  # Notifications push + WhatsApp customer status
  task-5.4.14-prompt.md  # Sync offline-first + retry queue
  task-5.4.15-prompt.md  # Tests E2E mobile 40+ + seeds tow operators fixtures
```

**Verification** : `V-22.5-sprint-22.5-verification.md`
**Decisions cles** : 012 (6 acteurs ecosystem) + 011 (rebrand) + Sprint 7.5a roles tow_*

---

## REGLES D'EXECUTION CRITIQUES

Sequentielle obligatoire (compile + tests + lint + commit avant tache suivante).

### Si une tache echoue : 3 tentatives reparation puis FAIL + continuer.

### Verification finale : V-22.5 automatique apres 15 taches.

---

## REGLES ABSOLUES skalean-insurtech v3.0

(Identique C-14/C-21/C-24 -- multi-tenant + Zod + Pino + Kafka + RBAC + Tests Vitest + TypeScript strict + pnpm + @insurtech/* + AUCUNE EMOJI + Idempotency-Key + Conventional Commits)

**Specifique Sprint 22.5 v3.0** :
- **React Native 0.74+** (cross-platform iOS + Android)
- **Expo SDK 51** (managed workflow pour velocite v3.0)
- **React Navigation 6** (stack + tabs)
- **TanStack Query 5** (cache + retry + offline persistence)
- **AsyncStorage + WatermelonDB** (offline-first sync)
- **react-native-maps + Mapbox** (GPS tracking)
- **Expo Camera + ImagePicker** (photos)
- **Expo Notifications** (push notifications)
- **Workflow 7 etats mission** : designated -> accepted -> en_route -> on_site -> towing -> delivered -> closed
- **Permissions Sprint 7.5a** : `tow.missions.read/accept/reject/update_status` + `tow.honoraires.read`

---

## CONTEXTE PHASE 5 -- Vertical Repair

### Position du Sprint 4 dans la Phase 5

Sprint 22.5 (5.4) -- Tow App -- 4eme sprint Phase 5 (apres Sprint 19 Repair Foundation + Sprint 20 IA Estimation + Sprint 20.5 Sky AI Pre-Training + Sprint 21 Sinistre Workflow + PartsHub + Sprint 22 Web Garage App).

### Modules concernes

@insurtech/tow (NOUVEAU package v3.0), @insurtech/database, @insurtech/auth, @insurtech/comm, apps/web-tow-app (NOUVEAU React Native app)

### Apport metier

Acteur Tow opere via mobile app + missions designees par carriers + visibilite customer real-time GPS tracking.

---

## EXECUTION SEQUENTIELLE DES 15 TACHES

---

### Tache 1 / 15 : Bootstrap React Native + monorepo apps/web-tow-app

**Metadonnees** : P0 | 6h | Depend de : Sprint 22 (Web Garage App)

**But** : Bootstrap React Native + Expo SDK 51 + structure apps/web-tow-app dans monorepo + integration Turbo.

**Commande de lecture** :
```bash
cat skalean-insurtech/00-pilotage/prompts-taches/sprint-22.5-tow-app/task-5.4.1-prompt.md
```

**Actions principales attendues** :
- Dossier `repo/apps/web-tow-app/` (Expo managed workflow)
- `app.json` config + `eas.json` (Expo Application Services)
- TypeScript strict + biome lint
- React Navigation 6 setup (stack + tabs)
- TanStack Query 5 + AsyncStorage persistence
- Theme Sofidemy (couleurs brand decision-011 -- bleu marine + accent or)
- 8 ecrans squelette (Login + Missions + MissionDetails + EnRoute + OnSite + Towing + Delivered + Dashboard)
- Tests Jest + Testing Library setup

**Fichiers cibles principaux** :
  - `repo/apps/web-tow-app/app.json`
  - `repo/apps/web-tow-app/eas.json`
  - `repo/apps/web-tow-app/src/screens/*.tsx`
  - `repo/apps/web-tow-app/src/theme/sofidemy.ts`

**Criteres P0 cles** :
  - V1 (P0) : Expo SDK 51 + RN 0.74+
  - V2 (P0) : Theme Sofidemy applique
  - V3 (P0) : 8 ecrans squelette navigables
  - V4 (P0) : Build iOS + Android sans erreur

**Validation** :
```bash
cd repo/apps/web-tow-app
pnpm install
pnpm tsc --noEmit
pnpm expo prebuild --clean
cd ../../..
```

**Commit** :
```bash
git commit -m "feat(sprint-22.5): bootstrap react native expo + apps/web-tow-app

Task: 5.4.1
Sprint: 22.5 (Phase 5 / Sprint 4)
Phase: 5 -- Vertical Repair
Decisions: see B-22.5 v3.0 + decision-012"
```

---

### Tache 2 / 15 : tow_operators entity + KYB workflow

**Metadonnees** : P0 | 5h | Depend de : 5.4.1

**But** : Implementer `tow_operators` entity + workflow KYB onboarding (CIN chauffeur + carte grise camion-grue + permis remorquage + ICE entreprise + assurance RC pro).

**Actions principales** :
- Migration `tow_operators` (id + tenant_id + owner_user_id + full_name + cin_document_url + truck_plate + truck_registration_url + tow_permit_url + ice_number + insurance_doc_url + zones_active text[] + truck_capacity_kg + truck_type CHECK (light/medium/heavy) + status CHECK (active/pending_kyb/suspended/inactive) + kyb_reviewed_at + kyb_reviewed_by_user_id + total_missions + avg_rating + baseline_honoraire_mad + timestamps)
- Indexes : GIN zones_active + tenant_id + status
- RLS + FORCE active
- Service `tow-operators.service.ts` : onboardOperator / approveKyb / rejectKyb / suspend / searchOperators (par zone + truck_type + availability)
- Endpoints REST + permissions Sprint 7.5a (`tow.operators.onboard/approve_kyb`)
- Tests 15+

**Criteres P0** :
- V1 : Migration + RLS + GIN zones
- V2 : Service 5 methodes
- V3 : Endpoints 5 + permissions enforce
- V4 : Tests 15+ PASS

**Commit** :
```bash
git commit -m "feat(sprint-22.5): tow_operators entity + kyb workflow

Task: 5.4.2
Sprint: 22.5 (Phase 5 / Sprint 4)
Phase: 5 -- Vertical Repair
Decisions: decision-012 + sprint 7.5a tow roles"
```

---

### Tache 3 / 15 : tow_missions entity + workflow 7 etats

**Metadonnees** : P0 | 5h | Depend de : 5.4.2

**But** : Implementer `tow_missions` entity + workflow 7 etats (designated -> accepted -> en_route -> on_site -> towing -> delivered -> closed) + branches rejection/cancellation.

**Actions** :
- Migration `tow_missions` (id + tenant_id + carrier_tenant_id + tow_tenant_id + tow_operator_id + sinistre_id + customer_user_id + pickup_lat + pickup_lng + pickup_address + destination_garage_id + destination_lat + destination_lng + truck_type_required + estimated_distance_km + estimated_duration_min + honoraire_base_mad + honoraire_extras_mad + honoraire_total_mad + status CHECK 7 etats + designated_at + accepted_at + rejected_at + rejection_reason + en_route_at + on_site_at + towing_started_at + delivered_at + closed_at + cancelled_at + cancelled_reason + notes + timestamps)
- Indexes : tenant_id + carrier_tenant_id + tow_operator_id + sinistre_id + status + designated_at
- RLS + FORCE
- Service `tow-missions.service.ts` : designateMission (par carrier) + acceptMission + rejectMission + transitionStatus + cancelMission + listMyMissions
- Cross-tenant `carrier_to_tow_assignment` (Sprint 7.5a)
- Tests 18+

**Commit** :
```bash
git commit -m "feat(sprint-22.5): tow_missions entity + workflow 7 etats

Task: 5.4.3
Sprint: 22.5 (Phase 5 / Sprint 4)
Phase: 5 -- Vertical Repair"
```

---

### Tache 4 / 15 : Authentification + onboarding mobile

**Metadonnees** : P0 | 5h | Depend de : 5.4.3

**But** : Auth mobile (JWT @insurtech/auth Sprint 5) + onboarding wizard (upload docs KYB CIN/carte grise/permis/ICE/RC pro/photos camion).

**Actions** :
- Screen LoginScreen + RegisterScreen + KybUploadScreen wizard
- Integration @insurtech/auth (login email + password + OTP SMS)
- Upload S3-compatible (Expo FileSystem + signed URLs)
- Status check : pending_kyb -> active automatique apres approbation
- Tests 10+

**Commit** :
```bash
git commit -m "feat(sprint-22.5): authentification + onboarding kyb wizard mobile

Task: 5.4.4
Sprint: 22.5 (Phase 5 / Sprint 4)"
```

---

### Tache 5 / 15 : Screen 1 -- Missions list (designees + active + history)

**Metadonnees** : P0 | 4h | Depend de : 5.4.4

**But** : Ecran liste missions avec 3 tabs (Designees | Active | Historique) + filtres + pull-to-refresh + push notification reception.

**Actions** :
- MissionsListScreen + 3 tabs
- Card mission : sinistre_id + distance + honoraire estimee + carrier + customer
- Pull-to-refresh + infinite scroll
- Real-time updates via Expo Notifications
- Tests 8+

**Commit** :
```bash
git commit -m "feat(sprint-22.5): screen missions list + 3 tabs + push notifications

Task: 5.4.5"
```

---

### Tache 6 / 15 : Screen 2 -- Mission details + accept/reject

**Metadonnees** : P0 | 4h | Depend de : 5.4.5

**But** : Detail mission complet (pickup map + destination map + photos sinistre + estimation honoraire + delais) + actions Accept (transition vers en_route) ou Reject (avec raison obligatoire).

**Actions** :
- MissionDetailsScreen avec 2 maps (pickup + destination) + photos sinistre + meta data
- Action Accept : confirme + transition accepted -> en_route
- Action Reject : modal selection raison (vehicule trop lourd / hors zone / occupe / autre)
- Notification carrier sur reject

**Commit** :
```bash
git commit -m "feat(sprint-22.5): screen mission details + accept/reject actions

Task: 5.4.6"
```

---

### Tache 7 / 15 : Screen 3 -- Intervention en_route + GPS tracking

**Metadonnees** : P0 | 5h | Depend de : 5.4.6

**But** : Ecran tracking en route vers pickup avec map + ETA + position chauffeur real-time stockee toutes les 30s dans `tow_mission_locations`.

**Actions** :
- EnRouteScreen avec Mapbox + GPS real-time
- Service `gps-tracker.service.ts` : background location updates 30s + persist DB
- ETA calcul (Mapbox Directions API)
- Notification customer "Tow en route" (WhatsApp scope strict)
- Action "I arrived" -> transition en_route -> on_site
- Tests 8+

**Commit** :
```bash
git commit -m "feat(sprint-22.5): screen en_route + gps tracking real-time 30s

Task: 5.4.7"
```

---

### Tache 8 / 15 : Screen 4 -- On-site + photos vehicule etat initial

**Metadonnees** : P0 | 4h | Depend de : 5.4.7

**But** : A l'arrivee sur place, photos vehicule etat initial (8 angles obligatoires) + check-in customer presence + signature customer.

**Actions** :
- OnSiteScreen + camera Expo capture
- Checklist 8 angles (avant gauche/droit/centre + arriere gauche/droit/centre + lateral gauche/droit)
- Upload photos S3 + integration `tow_mission_photos` table
- Signature customer (composant SignatureCanvas)
- Action "Start towing" -> transition on_site -> towing
- Tests 8+

**Commit** :
```bash
git commit -m "feat(sprint-22.5): screen on_site + photos 8 angles + signature customer

Task: 5.4.8"
```

---

### Tache 9 / 15 : Screen 5 -- Towing + transit GPS tracking

**Metadonnees** : P0 | 4h | Depend de : 5.4.8

**But** : Ecran transit vers garage avec GPS tracking continu + ETA destination + notifications customer + garage.

**Actions** :
- TowingScreen avec map + GPS tracking
- ETA destination garage
- Notifications customer + garage 3 etats (started + halfway + arriving 5min)
- Tests 6+

**Commit** :
```bash
git commit -m "feat(sprint-22.5): screen towing + transit gps tracking

Task: 5.4.9"
```

---

### Tache 10 / 15 : Screen 6 -- Delivered + signature garage reception

**Metadonnees** : P0 | 4h | Depend de : 5.4.9

**But** : Arrivee garage : photo vehicule depose + signature garage_chef + transition towing -> delivered + auto-create reception sinistre (Sprint 21 Tache 5.3.1).

**Actions** :
- DeliveredScreen + photo depose + signature garage
- Auto-trigger Sprint 21 Tache 5.3.1 (reception garage) avec tow_mission_id linked
- Transition delivered -> closed apres confirmation garage
- Tests 6+

**Commit** :
```bash
git commit -m "feat(sprint-22.5): screen delivered + signature garage + auto-trigger reception sprint 21

Task: 5.4.10"
```

---

### Tache 11 / 15 : Screen 7 -- Honoraires + paiement carrier mock

**Metadonnees** : P0 | 5h | Depend de : 5.4.10

**But** : Apres delivery, generation facture honoraires + soumission carrier paiement (mock Sprint 22.5, reel Sprint 26.5 Carrier Portal).

**Actions** :
- Migration `tow_honoraires_invoices` (mission_id + amount_base + extras + total + invoice_pdf_url + carrier_approval_status + paid_at)
- HonorairesScreen : visualisation factures + status paiement
- Service `tow-honoraires.service.ts` : generateInvoice + submitToCarrier + listMyInvoices
- Mock `MockCarrierHonorairesApprovalService` (90% approved in 7-14 days / 5% rejected / 5% partial)
- Tests 10+

**Commit** :
```bash
git commit -m "feat(sprint-22.5): screen honoraires + facturation + mock carrier paiement

Task: 5.4.11"
```

---

### Tache 12 / 15 : Screen 8 -- Dashboard + KPIs personnel

**Metadonnees** : P0 | 4h | Depend de : 5.4.11

**But** : Dashboard tow operator : missions completed + total honoraires MAD + avg rating + missions par mois + zones les plus actives.

**Actions** :
- DashboardScreen avec recharts mobile (victory-native)
- KPIs : total_missions + total_honoraires_mad + avg_rating + missions_this_month
- Charts : missions par mois (12 derniers) + repartition par zones

**Commit** :
```bash
git commit -m "feat(sprint-22.5): screen dashboard + kpis tow operator

Task: 5.4.12"
```

---

### Tache 13 / 15 : Notifications push + WhatsApp customer status

**Metadonnees** : P0 | 4h | Depend de : 5.4.12

**But** : Notifications push Expo + integration Comm Sprint 9 WhatsApp customer (scope strict status only).

**Actions** :
- Service `tow-notifications.service.ts` : sendPushToOperator + sendWhatsAppCustomerStatus (whitelist enforced)
- 6 templates customer status (tow_designated / en_route / on_site / towing_started / delivered_to_garage / mission_closed)
- WhatsApp blacklist fields enforced (no amount + no honoraire + no cin)
- Tests 8+

**Commit** :
```bash
git commit -m "feat(sprint-22.5): notifications push + whatsapp customer scope strict

Task: 5.4.13
Decisions: correction saad #7 whatsapp scope"
```

---

### Tache 14 / 15 : Sync offline-first + retry queue

**Metadonnees** : P0 | 5h | Depend de : 5.4.13

**But** : Mode offline-first (WatermelonDB) + retry queue Kafka pour sync GPS + photos + transitions quand reseau revient.

**Actions** :
- Setup WatermelonDB schema sync (missions + locations + photos)
- Service `offline-sync.service.ts` : queueAction + retryQueue + conflictResolution (last-write-wins)
- Tests offline scenarios 6+

**Commit** :
```bash
git commit -m "feat(sprint-22.5): sync offline-first watermelondb + retry queue

Task: 5.4.14"
```

---

### Tache 15 / 15 : Tests E2E mobile 40+ + seeds tow operators fixtures

**Metadonnees** : P0 | 11h | Depend de : 5.4.14

**But** : Tests E2E mobile 40+ scenarios (Detox + Maestro) + seeds dev 5 tow operators fixtures realistes Maroc (Casablanca + Rabat + Marrakech + Tanger + Fes).

**Actions** :
- Tests E2E happy path : onboarding KYB + accept mission + GPS tracking + on_site photos + towing + delivered + honoraires
- Tests offline scenarios + sync
- Tests rejection / cancellation
- Seeds 5 tow operators fixtures realistes (CIN + plates + zones)
- Coverage Sprint 22.5 >= 85%

**Commit** :
```bash
git commit -m "test(sprint-22.5): tests e2e mobile 40+ + seeds 5 tow operators fixtures

Task: 5.4.15"
```

---

## SYNTHESE -- Cloture Sprint 22.5 v3.0

```bash
# 15 commits Sprint 22.5
git log --since="2 weeks ago" --pretty=format:"%s" -- repo/ | grep "Task: 5.4" | wc -l
# Attendu : 15

# 0 emoji
grep -rP "[\x{1F300}-\x{1F9FF}]" repo/ --include="*.ts" --include="*.tsx" --include="*.md" | wc -l
# Attendu : 0

# Lancer V-22.5
cat skalean-insurtech/00-pilotage/meta-prompts/phase-V-verification/V-22.5-sprint-22.5-verification.md

# Si V-22.5 GO (>= 95%)
git tag -a "sprint-22.5-complete-v3-tow-app" -m "Sprint 22.5 v3.0 Tow App complete

- App mobile React Native + Expo SDK 51
- 8 ecrans operationnels
- GPS tracking real-time
- Workflow 7 etats missions
- 40+ tests E2E mobile PASS
- Score V-22.5 >= 95% -- GO"

git push origin sprint-22.5-complete-v3-tow-app

# Build EAS production
eas build --platform all --profile production
```

---

## RESUME DU WORKFLOW

```
[Demarrage Sprint 22.5 v3.0]
   |
   v
[Taches 5.4.1-3 : Bootstrap RN + entities + workflow]
   |
   v
[Tache 5.4.4 : Auth + KYB wizard mobile]
   |
   v
[Taches 5.4.5-10 : 8 ecrans navigation principale]
   |
   v
[Tache 5.4.11 : Honoraires + mock carrier paiement]
   |
   v
[Tache 5.4.12 : Dashboard KPIs]
   |
   v
[Taches 5.4.13-14 : Notifications + offline sync]
   |
   v
[Tache 5.4.15 : Tests E2E 40+]
   |
   v
[V-22.5 verification automatique]
   |
   v
[Score >= 95%] -> GO -> tag -> Sprint 22.7 Expert App
```

**Duree totale** : 75 heures.
**Modules affectes** : @insurtech/tow (NOUVEAU), @insurtech/auth, @insurtech/comm, apps/web-tow-app (NOUVEAU React Native)
**Apport metier** : Acteur Tow operationnel via mobile + GPS tracking + workflow complet
**Prerequis Sprint 22.7** : Sprint 22.5 GO complet
**Sprint suivant** : Sprint 22.7 Expert App

---

## COMMANDES DE LANCEMENT

```bash
# Prerequis (Sprint 22 GO)
grep '^Statut.*GO' skalean-insurtech/sprint22-verify-report.md

# Lancement
claude-code \
  --orchestrator skalean-insurtech/00-pilotage/meta-prompts/phase-C-orchestration/C-22.5-sprint-22.5-tow-app.md \
  --reference-prompt skalean-insurtech/00-pilotage/meta-prompts/phase-B-tasks/B-22.5-sprint-22.5-tow-app.md \
  --verification skalean-insurtech/00-pilotage/meta-prompts/phase-V-verification/V-22.5-sprint-22.5-verification.md

# Suivi
tail -f skalean-insurtech/cowork-sprint-22.5.log
git log --oneline --since="2 weeks ago" -- repo/ | grep "Sprint: 22.5"

# Apres completion
cat skalean-insurtech/sprint22.5-verify-report.md
```

---

## NOTES IMPORTANTES POUR COWORK

1. **Lire B-22.5 v3.0 complet** AVANT generation prompts (NOUVEAU sprint critique)
2. **Expo SDK 51 + RN 0.74+ obligatoires** (compatibility iOS + Android)
3. **GPS tracking 30s background** = critique pour visibilite carrier + customer
4. **Offline-first WatermelonDB** = obligatoire (chauffeurs zones rurales sans 4G)
5. **WhatsApp scope strict** customer = decision Saad absolue
6. **Theme Sofidemy** = brand decision-011 (bleu marine + accent or)
7. **Seeds 5 tow operators Maroc** = realistic fixtures pour Demo Day Sprint 24
8. **EAS Build production** apres GO pour deploiement App Store + Play Store
9. **NE JAMAIS modifier 00-pilotage/** -- uniquement repo/

---

**Fin orchestrateur C-22.5 v3.0 -- Sprint 22.5 (5.4) Tow App.**

**Total taches** : 15 | **Effort** : ~75h | **Apport** : Acteur Tow mobile operationnel ecosystem 6 acteurs
