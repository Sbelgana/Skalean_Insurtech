# VERIFICATION SPRINT 22.5 v3.0 -- Phase 5 / Sprint 4 : Tow App (Depanneur Mobile)
# Auto-reparation active + Rapport final MD detaille
# 15 taches, 75 criteres extraits B-22.5 v3.0
# AUCUNE EMOJI AUTORISEE

**Version** : v3.0 (verification detaillee NOUVEAU sprint)
**Phase** : 5 -- Vertical Repair
**Sprint** : 22.5 / 40 (cumul v3.0)
**Reference meta-prompt** : `B-22.5-sprint-22.5-tow-app.md` v3.0
**Reference orchestrateur** : `C-22.5-sprint-22.5-tow-app.md`
**Total criteres** : 75 criteres taches + 11 transversaux + 4 specifiques mobile

---

Tu es **Claude Code (ou Cowork)**. Execute verification COMPLETE Sprint 22.5 v3.0 apres 15 taches.

---

## REGLES D'EXECUTION

(Identique V-14/V-21/V-24 -- ordre + auto-reparation + rapport consolide + jalon GO/CONDITIONNEL/NO-GO)

---

## PHASE DE PREPARATION

```bash
REPORT_FILE="sprint22.5-verify-report.md"
TIMESTAMP=$(date '+%Y-%m-%d %H:%M:%S')
RUN_ID=$(date '+%Y%m%d-%H%M%S')

cat > "$REPORT_FILE" << EOF
# Rapport de Verification - Sprint 22.5 v3.0 : Tow App

**Date** : $TIMESTAMP
**Run ID** : $RUN_ID
**Phase** : 5 -- Vertical Repair
**Sprint** : 22.5 (Phase 5 / Sprint 4)
**Reference B-22.5 v3.0** : 15 taches, 75 criteres
**Executeur** : Claude Code / Cowork

---
EOF

PASS=0; PASS_REPAIRED=0; FAIL=0; SKIP=0; WARN=0; TABLE_ROWS=""

add_row() {
  local id="$1" desc="$2" status="$3" details="$4"
  TABLE_ROWS="$TABLE_ROWS| $id | $desc | $status | $details |\n"
  case "$status" in
    PASS)    ((PASS++)) ;;
    "PASS*") ((PASS_REPAIRED++)) ;;
    FAIL)    ((FAIL++)) ;;
    SKIP)    ((SKIP++)) ;;
    WARN)    ((WARN++)) ;;
  esac
  echo "[$status] $id - $desc : $details"
}

DB_URL="${DATABASE_URL:-postgresql://insurtech_user:SecurePassword123!@localhost:5432/insurtech}"
pg_query() { psql "$DB_URL" -t -c "$1" 2>/dev/null | tr -d ' \n'; }
pg_query_raw() { psql "$DB_URL" -c "$1" 2>/dev/null; }
```

---

## VERIFICATIONS PAR TACHE (15 taches)

---

## TACHE 1/15 -- 5.4.1 : Bootstrap React Native + Expo

```bash
echo ""
echo "TACHE 5.4.1 : Bootstrap React Native + Expo"

# T01-F1 : Dossier apps/web-tow-app
if [ -d "repo/apps/web-tow-app" ]; then
  add_row "T01-F1" "Dossier apps/web-tow-app existe" "PASS" ""
else
  add_row "T01-F1" "Dossier apps/web-tow-app existe" "FAIL" "Manquant"
fi

# T01-F2 : app.json Expo config
if [ -f "repo/apps/web-tow-app/app.json" ]; then
  add_row "T01-F2" "app.json present" "PASS" ""
else
  add_row "T01-F2" "app.json present" "FAIL" "Manquant"
fi

# T01-V1 (P0) : Expo SDK 51
PKG="repo/apps/web-tow-app/package.json"
if [ -f "$PKG" ] && grep -qE "\"expo\":.*\"~?51\." "$PKG" 2>/dev/null; then
  add_row "T01-V1" "Expo SDK 51" "PASS" "SDK 51"
else
  add_row "T01-V1" "Expo SDK 51" "FAIL" "SDK non 51"
fi

# T01-V2 (P0) : RN 0.74+
if grep -qE "\"react-native\":.*\"0\.7[4-9]" "$PKG" 2>/dev/null; then
  add_row "T01-V2" "React Native 0.74+" "PASS" ""
else
  add_row "T01-V2" "React Native 0.74+" "WARN" "Version a verifier"
fi

# T01-V3 : Theme Sofidemy
THEME="repo/apps/web-tow-app/src/theme/sofidemy.ts"
if [ -f "$THEME" ] && grep -qE "navy|blue|gold|sofidemy" "$THEME" 2>/dev/null; then
  add_row "T01-V3" "Theme Sofidemy applique" "PASS" "Brand colors"
else
  add_row "T01-V3" "Theme Sofidemy applique" "FAIL" "Theme manquant ou incomplet"
fi

# T01-V4 : 8 ecrans squelette
SCREENS=$(find repo/apps/web-tow-app/src/screens -name "*.tsx" 2>/dev/null | wc -l)
if [ "$SCREENS" -ge 8 ]; then
  add_row "T01-V4" "8 ecrans squelette" "PASS" "$SCREENS ecrans"
else
  add_row "T01-V4" "8 ecrans squelette" "FAIL" "$SCREENS / 8"
fi
```

---

## TACHE 2/15 -- 5.4.2 : tow_operators entity + KYB

```bash
echo ""
echo "TACHE 5.4.2 : tow_operators + KYB"

# T02-F1 : Table tow_operators
if pg_query "SELECT 1 FROM information_schema.tables WHERE table_name = 'tow_operators'" | grep -q "1"; then
  add_row "T02-F1" "Table tow_operators" "PASS" "Existe"
else
  add_row "T02-F1" "Table tow_operators" "FAIL" "Manquante"
fi

# T02-V1 (P0) : RLS + FORCE
RLS=$(pg_query "SELECT relrowsecurity AND relforcerowsecurity FROM pg_class WHERE relname = 'tow_operators'")
if [ "$RLS" = "t" ]; then
  add_row "T02-V1" "RLS + FORCE active" "PASS" ""
else
  pg_query_raw "ALTER TABLE tow_operators ENABLE ROW LEVEL SECURITY; ALTER TABLE tow_operators FORCE ROW LEVEL SECURITY;"
  RLS2=$(pg_query "SELECT relrowsecurity AND relforcerowsecurity FROM pg_class WHERE relname = 'tow_operators'")
  [ "$RLS2" = "t" ] && add_row "T02-V1" "RLS + FORCE active" "PASS*" "Repare auto" || add_row "T02-V1" "RLS + FORCE active" "FAIL" "Non activable"
fi

# T02-V2 : Index GIN zones_active
GIN_ZONES=$(pg_query "SELECT COUNT(*) FROM pg_indexes WHERE indexname LIKE '%tow_operators%zones%' OR indexname LIKE '%tow_operators_zones%'")
[ "${GIN_ZONES:-0}" -ge 1 ] && add_row "T02-V2" "Index GIN zones_active" "PASS" "" || add_row "T02-V2" "Index GIN zones_active" "FAIL" ""

# T02-V3 : Service 5 methodes
SERVICE="repo/packages/tow/src/services/tow-operators.service.ts"
METHODS=(onboardOperator approveKyb rejectKyb suspendOperator searchOperators)
OK=0; for m in "${METHODS[@]}"; do grep -q "$m" "$SERVICE" 2>/dev/null && OK=$((OK + 1)); done
[ "$OK" -eq 5 ] && add_row "T02-V3" "Service 5 methodes" "PASS" "" || add_row "T02-V3" "Service 5 methodes" "FAIL" "$OK / 5"

# T02-V4 : Tests 15+
cd repo
T02_TESTS=$(pnpm vitest run --reporter=json packages/tow/src/services/tow-operators.service.spec.ts 2>/dev/null | jq '.numPassedTests // 0' 2>/dev/null || echo 0)
cd ..
[ "$T02_TESTS" -ge 15 ] && add_row "T02-V4" "Tests >= 15" "PASS" "$T02_TESTS tests" || add_row "T02-V4" "Tests >= 15" "FAIL" "$T02_TESTS / 15"
```

---

## TACHE 3/15 -- 5.4.3 : tow_missions entity + workflow 7 etats

```bash
echo ""
echo "TACHE 5.4.3 : tow_missions + workflow 7 etats"

# T03-F1 : Table tow_missions
if pg_query "SELECT 1 FROM information_schema.tables WHERE table_name = 'tow_missions'" | grep -q "1"; then
  add_row "T03-F1" "Table tow_missions" "PASS" ""
else
  add_row "T03-F1" "Table tow_missions" "FAIL" "Manquante"
fi

# T03-V1 (P0) : RLS + FORCE
RLS=$(pg_query "SELECT relrowsecurity AND relforcerowsecurity FROM pg_class WHERE relname = 'tow_missions'")
if [ "$RLS" = "t" ]; then
  add_row "T03-V1" "RLS + FORCE tow_missions" "PASS" ""
else
  pg_query_raw "ALTER TABLE tow_missions ENABLE ROW LEVEL SECURITY; ALTER TABLE tow_missions FORCE ROW LEVEL SECURITY;"
  add_row "T03-V1" "RLS + FORCE tow_missions" "PASS*" "Repare auto"
fi

# T03-V2 (P0) : Workflow 7 etats CHECK
STATES=$(pg_query_raw "\d tow_missions" 2>&1 | grep -cE "designated|accepted|en_route|on_site|towing|delivered|closed")
[ "${STATES:-0}" -ge 1 ] && add_row "T03-V2" "Workflow 7 etats CHECK" "PASS" "Constraint present" || add_row "T03-V2" "Workflow 7 etats CHECK" "FAIL" "Constraint manquante"

# T03-V3 : Service 6 methodes
SERVICE="repo/packages/tow/src/services/tow-missions.service.ts"
METHODS=(designateMission acceptMission rejectMission transitionStatus cancelMission listMyMissions)
OK=0; for m in "${METHODS[@]}"; do grep -q "$m" "$SERVICE" 2>/dev/null && OK=$((OK + 1)); done
[ "$OK" -eq 6 ] && add_row "T03-V3" "Service 6 methodes" "PASS" "" || add_row "T03-V3" "Service 6 methodes" "FAIL" "$OK / 6"

# T03-V4 : Cross-tenant carrier_to_tow_assignment
if grep -q "carrier_to_tow_assignment" "$SERVICE" 2>/dev/null; then
  add_row "T03-V4" "Cross-tenant carrier_to_tow_assignment" "PASS" ""
else
  add_row "T03-V4" "Cross-tenant carrier_to_tow_assignment" "FAIL" "Type absent"
fi

# T03-V5 : Tests 18+
cd repo
T03_TESTS=$(pnpm vitest run --reporter=json packages/tow/src/services/tow-missions.service.spec.ts 2>/dev/null | jq '.numPassedTests // 0' 2>/dev/null || echo 0)
cd ..
[ "$T03_TESTS" -ge 18 ] && add_row "T03-V5" "Tests >= 18" "PASS" "$T03_TESTS tests" || add_row "T03-V5" "Tests >= 18" "FAIL" "$T03_TESTS / 18"
```

---

## TACHE 4/15 -- 5.4.4 : Auth + onboarding mobile

```bash
echo ""
echo "TACHE 5.4.4 : Auth + KYB wizard mobile"

# T04-F1 : LoginScreen
[ -f "repo/apps/web-tow-app/src/screens/LoginScreen.tsx" ] && add_row "T04-F1" "LoginScreen present" "PASS" "" || add_row "T04-F1" "LoginScreen present" "FAIL" "Manquant"

# T04-F2 : KybUploadScreen
[ -f "repo/apps/web-tow-app/src/screens/KybUploadScreen.tsx" ] && add_row "T04-F2" "KybUploadScreen present" "PASS" "" || add_row "T04-F2" "KybUploadScreen present" "FAIL" "Manquant"

# T04-V1 : Integration @insurtech/auth
if grep -rqE "@insurtech/auth|jwtLogin" repo/apps/web-tow-app/src 2>/dev/null; then
  add_row "T04-V1" "Integration @insurtech/auth" "PASS" ""
else
  add_row "T04-V1" "Integration @insurtech/auth" "FAIL" "Auth manquant"
fi
```

---

## TACHES 5-10/15 -- Ecrans principaux missions workflow

```bash
echo ""
echo "TACHES 5.4.5-10 : Ecrans 1-6 missions workflow"

# T05-F1 : MissionsListScreen
[ -f "repo/apps/web-tow-app/src/screens/MissionsListScreen.tsx" ] && add_row "T05-F1" "MissionsListScreen" "PASS" "" || add_row "T05-F1" "MissionsListScreen" "FAIL" "Manquant"

# T06-F1 : MissionDetailsScreen
[ -f "repo/apps/web-tow-app/src/screens/MissionDetailsScreen.tsx" ] && add_row "T06-F1" "MissionDetailsScreen" "PASS" "" || add_row "T06-F1" "MissionDetailsScreen" "FAIL" "Manquant"

# T07-F1 : EnRouteScreen
[ -f "repo/apps/web-tow-app/src/screens/EnRouteScreen.tsx" ] && add_row "T07-F1" "EnRouteScreen" "PASS" "" || add_row "T07-F1" "EnRouteScreen" "FAIL" "Manquant"

# T07-V1 (P0) : GPS tracking 30s background
GPS_SERVICE="repo/apps/web-tow-app/src/services/gps-tracker.service.ts"
if [ -f "$GPS_SERVICE" ] && grep -qE "30000|30 \* 1000|setInterval.*30" "$GPS_SERVICE" 2>/dev/null; then
  add_row "T07-V1" "GPS tracking 30s background" "PASS" "Interval OK"
else
  add_row "T07-V1" "GPS tracking 30s background" "FAIL" "GPS service KO"
fi

# T07-V2 : Table tow_mission_locations
if pg_query "SELECT 1 FROM information_schema.tables WHERE table_name = 'tow_mission_locations'" | grep -q "1"; then
  add_row "T07-V2" "Table tow_mission_locations" "PASS" ""
else
  add_row "T07-V2" "Table tow_mission_locations" "FAIL" "Manquante"
fi

# T08-F1 : OnSiteScreen
[ -f "repo/apps/web-tow-app/src/screens/OnSiteScreen.tsx" ] && add_row "T08-F1" "OnSiteScreen" "PASS" "" || add_row "T08-F1" "OnSiteScreen" "FAIL" "Manquant"

# T08-V1 (P0) : Photos 8 angles checklist
ONSITE="repo/apps/web-tow-app/src/screens/OnSiteScreen.tsx"
if [ -f "$ONSITE" ] && grep -cE "front_left|front_center|front_right|rear_left|rear_center|rear_right|side_left|side_right" "$ONSITE" 2>/dev/null | grep -qE "^[6-9]|^[0-9]{2}"; then
  add_row "T08-V1" "Photos 8 angles checklist" "PASS" ""
else
  add_row "T08-V1" "Photos 8 angles checklist" "WARN" "Checklist a verifier"
fi

# T09-F1 : TowingScreen
[ -f "repo/apps/web-tow-app/src/screens/TowingScreen.tsx" ] && add_row "T09-F1" "TowingScreen" "PASS" "" || add_row "T09-F1" "TowingScreen" "FAIL" "Manquant"

# T10-F1 : DeliveredScreen
[ -f "repo/apps/web-tow-app/src/screens/DeliveredScreen.tsx" ] && add_row "T10-F1" "DeliveredScreen" "PASS" "" || add_row "T10-F1" "DeliveredScreen" "FAIL" "Manquant"

# T10-V1 : Auto-trigger Sprint 21 reception
if grep -rqE "trigger.*reception|sprint-21.*reception" repo/apps/web-tow-app/src 2>/dev/null; then
  add_row "T10-V1" "Auto-trigger Sprint 21 reception" "PASS" ""
else
  add_row "T10-V1" "Auto-trigger Sprint 21 reception" "WARN" "Integration a verifier"
fi
```

---

## TACHE 11/15 -- 5.4.11 : Honoraires + paiement carrier mock

```bash
echo ""
echo "TACHE 5.4.11 : Honoraires + mock carrier paiement"

# T11-F1 : Table tow_honoraires_invoices
if pg_query "SELECT 1 FROM information_schema.tables WHERE table_name = 'tow_honoraires_invoices'" | grep -q "1"; then
  add_row "T11-F1" "Table tow_honoraires_invoices" "PASS" ""
else
  add_row "T11-F1" "Table tow_honoraires_invoices" "FAIL" "Manquante"
fi

# T11-V1 : Service tow-honoraires 3 methodes
SERVICE="repo/packages/tow/src/services/tow-honoraires.service.ts"
METHODS=(generateInvoice submitToCarrier listMyInvoices)
OK=0; for m in "${METHODS[@]}"; do grep -q "$m" "$SERVICE" 2>/dev/null && OK=$((OK + 1)); done
[ "$OK" -eq 3 ] && add_row "T11-V1" "Service 3 methodes" "PASS" "" || add_row "T11-V1" "Service 3 methodes" "FAIL" "$OK / 3"

# T11-V2 : Mock realistic 90/5/5
MOCK="repo/packages/tow/src/services/mock-carrier-honoraires-approval.service.ts"
if [ -f "$MOCK" ] && grep -qE "0\.90|0\.05" "$MOCK" 2>/dev/null; then
  add_row "T11-V2" "Mock 90/5/5 realistic" "PASS" "Rates OK"
else
  add_row "T11-V2" "Mock 90/5/5 realistic" "WARN" "A verifier"
fi
```

---

## TACHE 12/15 -- 5.4.12 : Dashboard KPIs

```bash
echo ""
echo "TACHE 5.4.12 : Dashboard KPIs"

[ -f "repo/apps/web-tow-app/src/screens/DashboardScreen.tsx" ] && add_row "T12-F1" "DashboardScreen" "PASS" "" || add_row "T12-F1" "DashboardScreen" "FAIL" "Manquant"

# T12-V1 : victory-native charts
PKG="repo/apps/web-tow-app/package.json"
if [ -f "$PKG" ] && grep -q "victory-native" "$PKG" 2>/dev/null; then
  add_row "T12-V1" "victory-native charts" "PASS" ""
else
  add_row "T12-V1" "victory-native charts" "WARN" "Charts a verifier"
fi
```

---

## TACHE 13/15 -- 5.4.13 : Notifications push + WhatsApp scope strict

```bash
echo ""
echo "TACHE 5.4.13 : Notifications + WhatsApp scope strict"

SERVICE="repo/packages/tow/src/services/tow-notifications.service.ts"

# T13-V1 : Service 2 methodes
if [ -f "$SERVICE" ]; then
  if grep -qE "sendPushToOperator|sendWhatsAppCustomerStatus" "$SERVICE"; then
    add_row "T13-V1" "Service notifications methodes" "PASS" ""
  else
    add_row "T13-V1" "Service notifications methodes" "FAIL" "Methodes manquantes"
  fi
else
  add_row "T13-V1" "Service notifications methodes" "FAIL" "Service manquant"
fi

# T13-V2 (P0 CRITIQUE) : WhatsApp scope strict (whitelist + blacklist)
if grep -qE "STATUS_ONLY_TEMPLATES|blacklistedFields" "$SERVICE" 2>/dev/null; then
  add_row "T13-V2" "WhatsApp scope strict (whitelist + blacklist)" "PASS" "Enforced"
else
  add_row "T13-V2" "WhatsApp scope strict" "FAIL" "Scope non strict (CNDP risk)"
fi

# T13-V3 : 6 templates customer status
TEMPLATES=$(find repo/packages/comm/src/templates -name "tow-*.hbs" 2>/dev/null | wc -l)
[ "$TEMPLATES" -ge 6 ] && add_row "T13-V3" "6+ templates tow-* customer" "PASS" "$TEMPLATES templates" || add_row "T13-V3" "6+ templates tow-* customer" "WARN" "$TEMPLATES / 6"
```

---

## TACHE 14/15 -- 5.4.14 : Sync offline-first WatermelonDB

```bash
echo ""
echo "TACHE 5.4.14 : Sync offline-first"

# T14-V1 : WatermelonDB integration
PKG="repo/apps/web-tow-app/package.json"
if grep -q "@nozbe/watermelondb" "$PKG" 2>/dev/null; then
  add_row "T14-V1" "WatermelonDB integration" "PASS" "Package present"
else
  add_row "T14-V1" "WatermelonDB integration" "FAIL" "Package absent"
fi

# T14-V2 : Service offline-sync
SYNC="repo/apps/web-tow-app/src/services/offline-sync.service.ts"
[ -f "$SYNC" ] && add_row "T14-V2" "offline-sync.service" "PASS" "" || add_row "T14-V2" "offline-sync.service" "FAIL" "Manquant"
```

---

## TACHE 15/15 -- 5.4.15 : Tests E2E mobile 40+

```bash
echo ""
echo "TACHE 5.4.15 : Tests E2E mobile 40+"

cd repo/apps/web-tow-app
TESTS=$(pnpm test --reporter=json 2>/dev/null | jq '.numPassedTests // 0' 2>/dev/null || echo 0)
cd ../../..
if [ "$TESTS" -ge 40 ]; then
  add_row "T15-V1" "Tests E2E mobile >= 40" "PASS" "$TESTS tests"
elif [ "$TESTS" -ge 30 ]; then
  add_row "T15-V1" "Tests E2E mobile >= 40" "WARN" "$TESTS / 40 (tolere)"
else
  add_row "T15-V1" "Tests E2E mobile >= 40" "FAIL" "$TESTS / 40"
fi

# T15-V2 : Seeds 5 tow operators
SEEDS="repo/infrastructure/scripts/seed-tow-operators-fixtures.ts"
if [ -f "$SEEDS" ] && grep -cE "Casablanca|Rabat|Marrakech|Tanger|Fes" "$SEEDS" 2>/dev/null | grep -qE "^[3-9]"; then
  add_row "T15-V2" "Seeds 5 tow operators Maroc" "PASS" "5 cities"
else
  add_row "T15-V2" "Seeds 5 tow operators Maroc" "WARN" "Seeds incomplets"
fi
```

---

## VERIFICATIONS TRANSVERSALES SPRINT 22.5

```bash
echo ""
echo "TRANSVERSAUX SPRINT 22.5"

cd repo

# TR-BUILD
pnpm turbo run build 2>&1 > /tmp/build.log; BUILD_CODE=$?
[ $BUILD_CODE -eq 0 ] && add_row "TR-BUILD" "Build monorepo" "PASS" "" || add_row "TR-BUILD" "Build monorepo" "FAIL" "Erreurs"

# TR-TYPECHECK
pnpm tsc --noEmit 2>&1 > /tmp/tsc.log
TS_ERRORS=$(grep -c "error TS" /tmp/tsc.log)
[ "$TS_ERRORS" -eq 0 ] && add_row "TR-TYPECHECK" "TypeScript strict" "PASS" "" || add_row "TR-TYPECHECK" "TypeScript strict" "FAIL" "$TS_ERRORS"

# TR-LINT
pnpm lint 2>&1 > /tmp/lint.log; LINT_CODE=$?
[ $LINT_CODE -eq 0 ] && add_row "TR-LINT" "Biome lint" "PASS" "" || add_row "TR-LINT" "Biome lint" "WARN" "Erreurs"

cd ..

# TR-NO-EMOJI
EMOJI=$(grep -rP "[\x{1F300}-\x{1F9FF}]|[\x{2600}-\x{27BF}]" repo/apps/web-tow-app repo/packages/tow --include="*.ts" --include="*.tsx" --include="*.md" 2>/dev/null | wc -l)
[ "$EMOJI" -eq 0 ] && add_row "TR-NO-EMOJI" "0 emoji" "PASS" "" || add_row "TR-NO-EMOJI" "0 emoji" "FAIL" "$EMOJI"

# TR-CONSOLE
CONSOLE=$(grep -rn "console\.log\|console\.error" repo/apps/web-tow-app repo/packages/tow --include="*.ts" --include="*.tsx" 2>/dev/null | grep -v ".spec.ts" | wc -l)
[ "$CONSOLE" -eq 0 ] && add_row "TR-CONSOLE" "0 console.*" "PASS" "" || add_row "TR-CONSOLE" "0 console.*" "FAIL" "$CONSOLE"

# TR-RLS NEW TABLES
NEW_TABLES=(tow_operators tow_missions tow_mission_locations tow_honoraires_invoices)
RLS_OK=0
for t in "${NEW_TABLES[@]}"; do
  RLS=$(pg_query "SELECT relrowsecurity AND relforcerowsecurity FROM pg_class WHERE relname = '$t'")
  [ "$RLS" = "t" ] && RLS_OK=$((RLS_OK + 1))
done
[ "$RLS_OK" -eq 4 ] && add_row "TR-RLS" "RLS + FORCE 4 nouvelles tables" "PASS" "4/4" || add_row "TR-RLS" "RLS + FORCE 4 nouvelles tables" "FAIL" "$RLS_OK / 4"

# TR-COMMITS
NON_CONV=$(git log --since="2 weeks ago" --pretty=format:"%s" -- repo/apps/web-tow-app repo/packages/tow | grep -vE "^(feat|fix|docs|test|chore|refactor|perf)(\(.+\))?:" | wc -l)
[ "$NON_CONV" -eq 0 ] && add_row "TR-COMMITS" "Conventional commits" "PASS" "" || add_row "TR-COMMITS" "Conventional commits" "WARN" "$NON_CONV"
```

---

## VERIFICATIONS SPECIFIQUES MOBILE (4 criteres)

```bash
echo ""
echo "VERIFICATIONS SPECIFIQUES MOBILE"

# M-1 : EAS Build config
if [ -f "repo/apps/web-tow-app/eas.json" ]; then
  add_row "M-1" "EAS Build config" "PASS" ""
else
  add_row "M-1" "EAS Build config" "FAIL" "Manquant"
fi

# M-2 : GPS background permissions iOS + Android
APP_JSON="repo/apps/web-tow-app/app.json"
if [ -f "$APP_JSON" ] && grep -qE "UIBackgroundModes|ACCESS_BACKGROUND_LOCATION" "$APP_JSON" 2>/dev/null; then
  add_row "M-2" "GPS background permissions" "PASS" ""
else
  add_row "M-2" "GPS background permissions" "FAIL" "Permissions non declarees"
fi

# M-3 : Push notifications Expo
if [ -f "$APP_JSON" ] && grep -q "expo-notifications" "$APP_JSON" 2>/dev/null; then
  add_row "M-3" "Expo notifications plugin" "PASS" ""
else
  add_row "M-3" "Expo notifications plugin" "WARN" "A verifier"
fi

# M-4 : Camera + ImagePicker permissions
if grep -qE "NSCameraUsageDescription|android\.permission\.CAMERA" "$APP_JSON" 2>/dev/null; then
  add_row "M-4" "Camera permissions" "PASS" ""
else
  add_row "M-4" "Camera permissions" "FAIL" "Non declarees"
fi
```

---

## RAPPORT FINAL CONSOLIDE

```bash
TOTAL=$((PASS + PASS_REPAIRED + FAIL + SKIP + WARN))
SCORE_NUM=$((PASS + PASS_REPAIRED))
SCORE_PCT=$(echo "scale=1; $SCORE_NUM * 100 / $TOTAL" | bc -l 2>/dev/null || echo "0")

JALON="NO-GO"
if (( $(echo "$SCORE_PCT >= 95" | bc -l 2>/dev/null || echo 0) )); then
  JALON="GO"
elif (( $(echo "$SCORE_PCT >= 85" | bc -l 2>/dev/null || echo 0) )); then
  JALON="GO CONDITIONNEL"
fi

cat >> "$REPORT_FILE" << EOF

## Synthese finale

| Categorie | Nombre |
|-----------|--------|
| PASS | $PASS |
| PASS* (repare) | $PASS_REPAIRED |
| FAIL | $FAIL |
| WARN | $WARN |
| **TOTAL** | **$TOTAL** |

**Score** : $SCORE_PCT% ($SCORE_NUM / $TOTAL)
**Jalon** : $JALON

---

## Resultats detailles

| ID | Description | Statut | Details |
|----|-------------|--------|---------|
$(echo -e "$TABLE_ROWS")

EOF

case "$JALON" in
  GO) cat >> "$REPORT_FILE" << EOF

**GO** -- Sprint 22.5 v3.0 valide.

Actions :
1. \`git tag -a "sprint-22.5-complete-v3-tow-app" -m "Sprint 22.5 v3.0 -- score $SCORE_PCT%"\`
2. \`eas build --platform all --profile production\`
3. Lancer Sprint 22.7 Expert App
EOF
;;
  "GO CONDITIONNEL") cat >> "$REPORT_FILE" << EOF

**GO CONDITIONNEL** ($SCORE_PCT%).

Actions : documenter dette + lancer Sprint 22.7
EOF
;;
  NO-GO) cat >> "$REPORT_FILE" << EOF

**NO-GO** ($SCORE_PCT%).

Actions : reparation FAIL P0 + re-V-22.5 + NE PAS lancer Sprint 22.7
EOF
;;
esac

cat "$REPORT_FILE"
```

---

**Fin V-22.5 v3.0.** Total criteres : 75 + 11 transversaux + 4 mobile = 90 criteres.
