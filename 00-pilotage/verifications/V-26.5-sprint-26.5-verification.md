# VERIFICATION SPRINT 26.5 v3.0 -- Phase 6 / Sprint 2 : Carrier Portal
# Auto-reparation active + Rapport final MD detaille
# 13 taches, 70 criteres extraits B-26.5 v3.0
# AUCUNE EMOJI AUTORISEE

**Version** : v3.0 (verification detaillee NOUVEAU sprint)
**Phase** : 6 -- Admin + Carrier
**Sprint** : 26.5 / 40 (cumul v3.0)
**Reference meta-prompt** : `B-26.5-sprint-26.5-carrier-portal.md` v3.0
**Reference orchestrateur** : `C-26.5-sprint-26.5-carrier-portal.md`
**Total criteres** : 70 criteres taches + 11 transversaux + 3 specifiques replacement mocks

---

Tu es **Claude Code (ou Cowork)**. Execute verification COMPLETE Sprint 26.5 v3.0 apres 13 taches. **Jalon critique : replacement mocks Sprint 21 + 22.5 + 24 par feature flag USE_REAL_CARRIER_PORTAL.**

---

## PHASE DE PREPARATION

```bash
REPORT_FILE="sprint26.5-verify-report.md"
TIMESTAMP=$(date '+%Y-%m-%d %H:%M:%S')
RUN_ID=$(date '+%Y%m%d-%H%M%S')

cat > "$REPORT_FILE" << EOF
# Rapport de Verification - Sprint 26.5 v3.0 : Carrier Portal

**Date** : $TIMESTAMP
**Run ID** : $RUN_ID
**Phase** : 6 -- Admin + Carrier
**Sprint** : 26.5 (Phase 6 / Sprint 2)
**Reference B-26.5 v3.0** : 13 taches, 70 criteres
**Executeur** : Claude Code / Cowork

---
EOF

PASS=0; PASS_REPAIRED=0; FAIL=0; SKIP=0; WARN=0; TABLE_ROWS=""

add_row() {
  local id="$1" desc="$2" status="$3" details="$4"
  TABLE_ROWS="$TABLE_ROWS| $id | $desc | $status | $details |\n"
  case "$status" in
    PASS)    ((PASS++)) ;;
    "PASS*") ((PASS_REPAIRED++)) ;;
    FAIL)    ((FAIL++)) ;;
    SKIP)    ((SKIP++)) ;;
    WARN)    ((WARN++)) ;;
  esac
  echo "[$status] $id - $desc : $details"
}

DB_URL="${DATABASE_URL:-postgresql://insurtech_user:SecurePassword123!@localhost:5432/insurtech}"
pg_query() { psql "$DB_URL" -t -c "$1" 2>/dev/null | tr -d ' \n'; }
pg_query_raw() { psql "$DB_URL" -c "$1" 2>/dev/null; }
```

---

## VERIFICATIONS PAR TACHE (13 taches)

---

## TACHE 1/13 -- 6.2.1 : Bootstrap Next.js 14 + apps/web-carrier-portal

```bash
echo ""
echo "TACHE 6.2.1 : Bootstrap Next.js 14 + carrier portal"

# T01-F1 : Dossier
[ -d "repo/apps/web-carrier-portal" ] && add_row "T01-F1" "Dossier apps/web-carrier-portal" "PASS" "" || add_row "T01-F1" "Dossier apps/web-carrier-portal" "FAIL" "Manquant"

# T01-V1 : Next.js 14
PKG="repo/apps/web-carrier-portal/package.json"
if [ -f "$PKG" ] && grep -qE "\"next\":.*\"14\." "$PKG" 2>/dev/null; then
  add_row "T01-V1" "Next.js 14" "PASS" ""
else
  add_row "T01-V1" "Next.js 14" "FAIL" "Version non 14"
fi

# T01-V2 : Server Actions enabled
NEXT_CONFIG="repo/apps/web-carrier-portal/next.config.mjs"
if [ -f "$NEXT_CONFIG" ] && grep -qE "serverActions|experimental" "$NEXT_CONFIG" 2>/dev/null; then
  add_row "T01-V2" "Server Actions enabled" "PASS" ""
else
  add_row "T01-V2" "Server Actions enabled" "WARN" "A verifier"
fi

# T01-V3 : 9 routes squelette
ROUTES=$(find repo/apps/web-carrier-portal/app -name "page.tsx" 2>/dev/null | wc -l)
if [ "$ROUTES" -ge 9 ]; then
  add_row "T01-V3" "9 routes squelette" "PASS" "$ROUTES routes"
else
  add_row "T01-V3" "9 routes squelette" "FAIL" "$ROUTES / 9"
fi

# T01-V4 : Theme Sofidemy
THEME="repo/apps/web-carrier-portal/lib/theme/sofidemy.ts"
if [ -f "$THEME" ] || grep -qE "sofidemy|navy.*gold" "$NEXT_CONFIG" 2>/dev/null; then
  add_row "T01-V4" "Theme Sofidemy" "PASS" ""
else
  add_row "T01-V4" "Theme Sofidemy" "WARN" "Theme a verifier"
fi
```

---

## TACHE 2/13 -- 6.2.2 : Auth 6 roles carrier + onboarding

```bash
echo ""
echo "TACHE 6.2.2 : Auth 6 roles carrier + onboarding"

# T02-F1 : LoginPage
[ -f "repo/apps/web-carrier-portal/app/login/page.tsx" ] && add_row "T02-F1" "LoginPage" "PASS" "" || add_row "T02-F1" "LoginPage" "FAIL" "Manquant"

# T02-V1 (P0) : 6 roles carrier selectionnables
ONBOARDING=$(find repo/apps/web-carrier-portal -name "*onboarding*" -name "*.tsx" 2>/dev/null | head -3)
ROLES_OK=0
for f in $ONBOARDING; do
  ROLES_COUNT=$(grep -cE "carrier_admin|carrier_underwriter|carrier_claims_manager|carrier_expert_manager|carrier_finance|carrier_compliance" "$f" 2>/dev/null || echo 0)
  if [ "$ROLES_COUNT" -ge 4 ]; then
    ROLES_OK=1
    break
  fi
done
[ "$ROLES_OK" -eq 1 ] && add_row "T02-V1" "6 roles carrier selectionnables" "PASS" "" || add_row "T02-V1" "6 roles carrier selectionnables" "FAIL" "Roles incomplets"

# T02-V2 : KYB compagnie (ICE + ACAPS)
SERVICE="repo/apps/web-carrier-portal/lib/services/carrier-onboarding.service.ts"
if [ -f "$SERVICE" ] && grep -qE "ice_number|acaps_agrement|onboardCarrier" "$SERVICE" 2>/dev/null; then
  add_row "T02-V2" "KYB compagnie (ICE + ACAPS)" "PASS" ""
else
  add_row "T02-V2" "KYB compagnie (ICE + ACAPS)" "FAIL" "KYB manquant"
fi

# T02-V3 : Service 3 methodes
METHODS=(onboardCarrier inviteMember setPermissions)
OK=0; for m in "${METHODS[@]}"; do grep -q "$m" "$SERVICE" 2>/dev/null && OK=$((OK + 1)); done
[ "$OK" -eq 3 ] && add_row "T02-V3" "Service 3 methodes" "PASS" "" || add_row "T02-V3" "Service 3 methodes" "FAIL" "$OK / 3"

# T02-V4 : Tests 12+
cd repo
T02_TESTS=$(pnpm vitest run --reporter=json apps/web-carrier-portal/test/onboarding.spec.ts 2>/dev/null | jq '.numPassedTests // 0' 2>/dev/null || echo 0)
cd ..
[ "$T02_TESTS" -ge 12 ] && add_row "T02-V4" "Tests >= 12" "PASS" "$T02_TESTS" || add_row "T02-V4" "Tests >= 12" "FAIL" "$T02_TESTS / 12"
```

---

## TACHE 3/13 -- 6.2.3 : Screen sinistres dashboard

```bash
echo ""
echo "TACHE 6.2.3 : Sinistres dashboard + data table"

# T03-F1 : Page sinistres
[ -f "repo/apps/web-carrier-portal/app/sinistres/page.tsx" ] && add_row "T03-F1" "/sinistres page" "PASS" "" || add_row "T03-F1" "/sinistres page" "FAIL" "Manquant"

# T03-V1 : TanStack Table integration
PAGE="repo/apps/web-carrier-portal/app/sinistres/page.tsx"
if [ -f "$PAGE" ] && grep -qE "@tanstack/react-table|useReactTable" "$PAGE" 2>/dev/null; then
  add_row "T03-V1" "TanStack Table integration" "PASS" ""
else
  add_row "T03-V1" "TanStack Table integration" "WARN" "A verifier"
fi

# T03-V2 : Filtres + tri + pagination
if [ -f "$PAGE" ] && grep -cE "filter|sort|pagination" "$PAGE" 2>/dev/null | grep -qE "^[3-9]"; then
  add_row "T03-V2" "Filtres + tri + pagination" "PASS" ""
else
  add_row "T03-V2" "Filtres + tri + pagination" "WARN" "Partiel"
fi

# T03-V3 : SSE real-time
if grep -rqE "EventSource|useSSE|/api/sse" repo/apps/web-carrier-portal/app/sinistres 2>/dev/null; then
  add_row "T03-V3" "SSE real-time updates" "PASS" ""
else
  add_row "T03-V3" "SSE real-time updates" "WARN" "A verifier"
fi
```

---

## TACHE 4/13 -- 6.2.4 : CRITIQUE -- REPLACEMENT MockCarrierFnolService

```bash
echo ""
echo "TACHE 6.2.4 : CRITIQUE REPLACEMENT MockCarrierFnolService"

# T04-F1 : Page sinistres/[id]
[ -f "repo/apps/web-carrier-portal/app/sinistres/[id]/page.tsx" ] && add_row "T04-F1" "/sinistres/[id]" "PASS" "" || add_row "T04-F1" "/sinistres/[id]" "FAIL" "Manquant"

# T04-V1 (P0 CRITIQUE) : Service replace MockCarrierFnolService Sprint 24
SERVICE="repo/apps/web-carrier-portal/lib/services/carrier-fnol-review.service.ts"
if [ -f "$SERVICE" ] && grep -qE "reviewFnol|requestSkyAiRoutingRecommendation|routeSinistre" "$SERVICE" 2>/dev/null; then
  add_row "T04-V1" "Service CarrierFnolReviewService (replace mock S24)" "PASS" "Service present"
else
  add_row "T04-V1" "Service CarrierFnolReviewService (replace mock S24)" "FAIL" "Service replacement absent (CRITIQUE)"
fi

# T04-V2 (P0) : Service 3 methodes
METHODS=(reviewFnol requestSkyAiRoutingRecommendation routeSinistre)
OK=0; for m in "${METHODS[@]}"; do grep -q "$m" "$SERVICE" 2>/dev/null && OK=$((OK + 1)); done
[ "$OK" -eq 3 ] && add_row "T04-V2" "Service 3 methodes" "PASS" "" || add_row "T04-V2" "Service 3 methodes" "FAIL" "$OK / 3"

# T04-V3 (P0 CRITIQUE) : Feature flag USE_REAL_CARRIER_PORTAL
ENV_USAGE=$(grep -rE "USE_REAL_CARRIER_PORTAL" repo/apps/web-carrier-portal repo/packages 2>/dev/null | wc -l)
if [ "${ENV_USAGE:-0}" -ge 2 ]; then
  add_row "T04-V3" "Feature flag USE_REAL_CARRIER_PORTAL" "PASS" "$ENV_USAGE usages"
else
  add_row "T04-V3" "Feature flag USE_REAL_CARRIER_PORTAL" "FAIL" "Flag absent ou insuffisant"
fi

# T04-V4 (P0) : Sky AI integration
if grep -qE "@insurtech/sky|skyAiDecisionEngine" "$SERVICE" 2>/dev/null; then
  add_row "T04-V4" "Sky AI Decision Engine integre" "PASS" ""
else
  add_row "T04-V4" "Sky AI Decision Engine integre" "FAIL" "Sky AI absent"
fi

# T04-V5 (P0) : Audit ACAPS log
if grep -qE "compliance_acaps_audits|acapsAuditService" "$SERVICE" 2>/dev/null; then
  add_row "T04-V5" "Audit ACAPS log automatique" "PASS" ""
else
  add_row "T04-V5" "Audit ACAPS log automatique" "FAIL" "Audit absent"
fi

# T04-V6 : Tests 12+
cd repo
T04_TESTS=$(pnpm vitest run --reporter=json apps/web-carrier-portal/test/fnol-review.spec.ts 2>/dev/null | jq '.numPassedTests // 0' 2>/dev/null || echo 0)
cd ..
[ "$T04_TESTS" -ge 12 ] && add_row "T04-V6" "Tests >= 12" "PASS" "$T04_TESTS" || add_row "T04-V6" "Tests >= 12" "FAIL" "$T04_TESTS / 12"
```

---

## TACHE 5/13 -- 6.2.5 : Designation expert pool Sprint 14

```bash
echo ""
echo "TACHE 6.2.5 : Designation expert pool Sprint 14"

# T05-F1 : Page designate-expert
[ -f "repo/apps/web-carrier-portal/app/experts/designate/page.tsx" ] && add_row "T05-F1" "/experts/designate" "PASS" "" || add_row "T05-F1" "/experts/designate" "FAIL" "Manquant"

# T05-V1 : Service wrapper Sprint 14
SERVICE="repo/apps/web-carrier-portal/lib/services/carrier-expert-designation.service.ts"
if [ -f "$SERVICE" ] && grep -qE "expertAssignmentsService|expert-assignments\.service|insure_experts" "$SERVICE" 2>/dev/null; then
  add_row "T05-V1" "Wrapper Sprint 14 expert-assignments" "PASS" ""
else
  add_row "T05-V1" "Wrapper Sprint 14 expert-assignments" "FAIL" "Integration manquante"
fi

# T05-V2 : Catalog filtres (specialty + zone + rating)
PAGE="repo/apps/web-carrier-portal/app/experts/designate/page.tsx"
if [ -f "$PAGE" ] && grep -cE "specialty|zone|rating" "$PAGE" 2>/dev/null | grep -qE "^[3-9]"; then
  add_row "T05-V2" "Catalog filtres" "PASS" ""
else
  add_row "T05-V2" "Catalog filtres" "WARN" "Filtres partiels"
fi

# T05-V3 : Permission carrier.experts.designate
if grep -rqE "carrier\.experts\.designate" repo/apps/web-carrier-portal 2>/dev/null; then
  add_row "T05-V3" "Permission carrier.experts.designate" "PASS" ""
else
  add_row "T05-V3" "Permission carrier.experts.designate" "FAIL" "Permission absente"
fi
```

---

## TACHE 6/13 -- 6.2.6 : Designation tow pool Sprint 22.5

```bash
echo ""
echo "TACHE 6.2.6 : Designation tow pool Sprint 22.5"

# T06-F1 : Page designate-tow
[ -f "repo/apps/web-carrier-portal/app/tow/designate/page.tsx" ] && add_row "T06-F1" "/tow/designate" "PASS" "" || add_row "T06-F1" "/tow/designate" "FAIL" "Manquant"

# T06-V1 : Service wrapper Sprint 22.5
SERVICE="repo/apps/web-carrier-portal/lib/services/carrier-tow-designation.service.ts"
if [ -f "$SERVICE" ] && grep -qE "tow_missions|tow-missions\.service" "$SERVICE" 2>/dev/null; then
  add_row "T06-V1" "Wrapper Sprint 22.5 tow-missions" "PASS" ""
else
  add_row "T06-V1" "Wrapper Sprint 22.5 tow-missions" "FAIL" "Integration manquante"
fi

# T06-V2 : Map view (geo visualisation)
PAGE="repo/apps/web-carrier-portal/app/tow/designate/page.tsx"
if [ -f "$PAGE" ] && grep -qE "Map|MapBox|leaflet|react-leaflet" "$PAGE" 2>/dev/null; then
  add_row "T06-V2" "Map view geo" "PASS" ""
else
  add_row "T06-V2" "Map view geo" "WARN" "Map a verifier"
fi
```

---

## TACHE 7/13 -- 6.2.7 : Validation rapports experts

```bash
echo ""
echo "TACHE 6.2.7 : Validation rapports experts (Sprint 22.7 reception)"

# T07-F1 : Page reports review
[ -f "repo/apps/web-carrier-portal/app/reports/review/page.tsx" ] && add_row "T07-F1" "/reports/review" "PASS" "" || add_row "T07-F1" "/reports/review" "FAIL" "Manquant"

# T07-V1 : Service 4 methodes
SERVICE="repo/apps/web-carrier-portal/lib/services/carrier-expert-reports-review.service.ts"
METHODS=(listPendingReports getReportDetails acceptReport contestReport)
OK=0; for m in "${METHODS[@]}"; do grep -q "$m" "$SERVICE" 2>/dev/null && OK=$((OK + 1)); done
[ "$OK" -eq 4 ] && add_row "T07-V1" "Service 4 methodes" "PASS" "" || add_row "T07-V1" "Service 4 methodes" "FAIL" "$OK / 4"

# T07-V2 : Transition status accepted_by_carrier
if grep -qE "accepted_by_carrier" "$SERVICE" 2>/dev/null; then
  add_row "T07-V2" "Transition accepted_by_carrier" "PASS" ""
else
  add_row "T07-V2" "Transition accepted_by_carrier" "FAIL" "Status manquant"
fi

# T07-V3 : Permissions enforces
if grep -rqE "carrier\.expert_reports\.(review|accept|contest)" repo/apps/web-carrier-portal 2>/dev/null; then
  add_row "T07-V3" "Permissions expert_reports.*" "PASS" ""
else
  add_row "T07-V3" "Permissions expert_reports.*" "FAIL" "Permissions absentes"
fi

# T07-V4 : Tests 10+
cd repo
T07_TESTS=$(pnpm vitest run --reporter=json apps/web-carrier-portal/test/expert-reports-review.spec.ts 2>/dev/null | jq '.numPassedTests // 0' 2>/dev/null || echo 0)
cd ..
[ "$T07_TESTS" -ge 10 ] && add_row "T07-V4" "Tests >= 10" "PASS" "$T07_TESTS" || add_row "T07-V4" "Tests >= 10" "FAIL" "$T07_TESTS / 10"
```

---

## TACHE 8/13 -- 6.2.8 : CRITIQUE -- REPLACEMENT MockCarrierPaymentService

```bash
echo ""
echo "TACHE 6.2.8 : CRITIQUE REPLACEMENT MockCarrierPaymentService Sprint 21"

# T08-F1 : Page approval
[ -f "repo/apps/web-carrier-portal/app/payments/approval/page.tsx" ] && add_row "T08-F1" "/payments/approval" "PASS" "" || add_row "T08-F1" "/payments/approval" "FAIL" "Manquant"

# T08-V1 (P0 CRITIQUE) : Service replace MockCarrierPaymentService
SERVICE="repo/apps/web-carrier-portal/lib/services/carrier-payment-approval.service.ts"
if [ -f "$SERVICE" ] && grep -qE "approveDeviceLevel1|approveDeviceLevel2|listPendingApprovals" "$SERVICE" 2>/dev/null; then
  add_row "T08-V1" "Service CarrierPaymentApprovalService (replace mock S21)" "PASS" ""
else
  add_row "T08-V1" "Service CarrierPaymentApprovalService (replace mock S21)" "FAIL" "Service replacement absent (CRITIQUE)"
fi

# T08-V2 (P0) : Service 5 methodes
METHODS=(listPendingApprovals approveDeviceLevel1 approveDeviceLevel2 rejectDevisPayment notifyPaymentInitiated)
OK=0; for m in "${METHODS[@]}"; do grep -q "$m" "$SERVICE" 2>/dev/null && OK=$((OK + 1)); done
[ "$OK" -eq 5 ] && add_row "T08-V2" "Service 5 methodes" "PASS" "" || add_row "T08-V2" "Service 5 methodes" "FAIL" "$OK / 5"

# T08-V3 (P0 CRITIQUE) : Multi-level approval > 50k MAD
if grep -qE "50000|50_000|threshold.*mad" "$SERVICE" 2>/dev/null; then
  add_row "T08-V3" "Multi-level approval > 50k MAD" "PASS" ""
else
  add_row "T08-V3" "Multi-level approval > 50k MAD" "WARN" "Threshold a verifier"
fi

# T08-V4 (P0) : decimal.js precision
if grep -qE "import.*Decimal|new Decimal" "$SERVICE" 2>/dev/null; then
  add_row "T08-V4" "decimal.js precision" "PASS" ""
else
  add_row "T08-V4" "decimal.js precision" "FAIL" "decimal.js MANQUANT"
fi

# T08-V5 (P0) : Audit ACAPS automatique
if grep -qE "compliance_acaps_audits|acapsAuditService" "$SERVICE" 2>/dev/null; then
  add_row "T08-V5" "Audit ACAPS automatique" "PASS" ""
else
  add_row "T08-V5" "Audit ACAPS automatique" "FAIL" "Audit absent (compliance loi)"
fi

# T08-V6 : Tests 12+
cd repo
T08_TESTS=$(pnpm vitest run --reporter=json apps/web-carrier-portal/test/payment-approval.spec.ts 2>/dev/null | jq '.numPassedTests // 0' 2>/dev/null || echo 0)
cd ..
[ "$T08_TESTS" -ge 12 ] && add_row "T08-V6" "Tests >= 12" "PASS" "$T08_TESTS" || add_row "T08-V6" "Tests >= 12" "FAIL" "$T08_TESTS / 12"
```

---

## TACHE 9/13 -- 6.2.9 : REPLACEMENT MockCarrierHonorairesApprovalService Sprint 22.5

```bash
echo ""
echo "TACHE 6.2.9 : REPLACEMENT MockCarrierHonorairesApprovalService Sprint 22.5"

# T09-F1 : Page honoraires approval
[ -f "repo/apps/web-carrier-portal/app/honoraires/approval/page.tsx" ] && add_row "T09-F1" "/honoraires/approval" "PASS" "" || add_row "T09-F1" "/honoraires/approval" "FAIL" "Manquant"

# T09-V1 (P0) : Service replace mock S22.5
SERVICE="repo/apps/web-carrier-portal/lib/services/carrier-honoraires-approval.service.ts"
if [ -f "$SERVICE" ] && grep -qE "approveExpertHonoraire|approveTowHonoraire" "$SERVICE" 2>/dev/null; then
  add_row "T09-V1" "Service CarrierHonorairesApprovalService (replace mock S22.5)" "PASS" ""
else
  add_row "T09-V1" "Service replace mock S22.5" "FAIL" "Service absent"
fi

# T09-V2 : 2 tabs experts + tow
PAGE="repo/apps/web-carrier-portal/app/honoraires/approval/page.tsx"
if [ -f "$PAGE" ] && grep -cE "experts|tow|Tabs" "$PAGE" 2>/dev/null | grep -qE "^[2-9]"; then
  add_row "T09-V2" "2 tabs experts + tow" "PASS" ""
else
  add_row "T09-V2" "2 tabs experts + tow" "WARN" "Tabs a verifier"
fi

# T09-V3 : Integration Sprint 11 Pay
if grep -qE "@insurtech/pay|payService|initiatePayment" "$SERVICE" 2>/dev/null; then
  add_row "T09-V3" "Integration Sprint 11 Pay" "PASS" ""
else
  add_row "T09-V3" "Integration Sprint 11 Pay" "FAIL" "Pay integration absente"
fi

# T09-V4 : Service 6 methodes
METHODS=(listPendingExpertHonoraires listPendingTowHonoraires approveExpertHonoraire approveTowHonoraire rejectHonoraire initiatePayment)
OK=0; for m in "${METHODS[@]}"; do grep -q "$m" "$SERVICE" 2>/dev/null && OK=$((OK + 1)); done
[ "$OK" -eq 6 ] && add_row "T09-V4" "Service 6 methodes" "PASS" "" || add_row "T09-V4" "Service 6 methodes" "FAIL" "$OK / 6"
```

---

## TACHE 10/13 -- 6.2.10 : Dashboard KPIs + analytics

```bash
echo ""
echo "TACHE 6.2.10 : Dashboard KPIs + analytics"

# T10-F1 : Page dashboard
[ -f "repo/apps/web-carrier-portal/app/dashboard/page.tsx" ] && add_row "T10-F1" "/dashboard" "PASS" "" || add_row "T10-F1" "/dashboard" "FAIL" "Manquant"

# T10-V1 : Service analytics
SERVICE="repo/apps/web-carrier-portal/lib/services/carrier-analytics.service.ts"
METHODS=(computeDashboardKpis getCharts getAlertsPanel)
OK=0; for m in "${METHODS[@]}"; do grep -q "$m" "$SERVICE" 2>/dev/null && OK=$((OK + 1)); done
[ "$OK" -eq 3 ] && add_row "T10-V1" "Service 3 methodes" "PASS" "" || add_row "T10-V1" "Service 3 methodes" "FAIL" "$OK / 3"

# T10-V2 : recharts integration
PAGE="repo/apps/web-carrier-portal/app/dashboard/page.tsx"
if [ -f "$PAGE" ] && grep -qE "recharts|BarChart|LineChart" "$PAGE" 2>/dev/null; then
  add_row "T10-V2" "recharts integration" "PASS" ""
else
  add_row "T10-V2" "recharts integration" "WARN" "Charts a verifier"
fi

# T10-V3 : Alerts panel
if [ -f "$PAGE" ] && grep -qE "alerts_panel|getAlertsPanel|AlertsPanel" "$PAGE" 2>/dev/null; then
  add_row "T10-V3" "Alerts panel (bloques + agrement + overdue)" "PASS" ""
else
  add_row "T10-V3" "Alerts panel" "WARN" "Alerts a verifier"
fi
```

---

## TACHE 11/13 -- 6.2.11 : Reports compliance ACAPS export

```bash
echo ""
echo "TACHE 6.2.11 : Reports compliance ACAPS export"

# T11-F1 : Page compliance reports
[ -f "repo/apps/web-carrier-portal/app/reports/compliance/page.tsx" ] && add_row "T11-F1" "/reports/compliance" "PASS" "" || add_row "T11-F1" "/reports/compliance" "FAIL" "Manquant"

# T11-V1 : Service compliance reports
SERVICE="repo/apps/web-carrier-portal/lib/services/carrier-compliance-reports.service.ts"
METHODS=(generateMonthlyReport generateQuarterlyReport generateAnnualReport exportXmlAcaps)
OK=0; for m in "${METHODS[@]}"; do grep -q "$m" "$SERVICE" 2>/dev/null && OK=$((OK + 1)); done
[ "$OK" -eq 4 ] && add_row "T11-V1" "Service 4 methodes (M/Q/A + ACAPS XML)" "PASS" "" || add_row "T11-V1" "Service 4 methodes" "FAIL" "$OK / 4"

# T11-V2 : Permission carrier.compliance.export
if grep -rqE "carrier\.compliance\.export" repo/apps/web-carrier-portal 2>/dev/null; then
  add_row "T11-V2" "Permission carrier.compliance.export" "PASS" ""
else
  add_row "T11-V2" "Permission carrier.compliance.export" "FAIL" "Permission absente"
fi

# T11-V3 : Formats (PDF/Excel/XML)
if [ -f "$SERVICE" ] && grep -cE "pdf|excel|xml" "$SERVICE" 2>/dev/null | grep -qE "^[3-9]"; then
  add_row "T11-V3" "3 formats (PDF + Excel + XML)" "PASS" ""
else
  add_row "T11-V3" "3 formats (PDF + Excel + XML)" "WARN" "Formats partiels"
fi
```

---

## TACHE 12/13 -- 6.2.12 : Multi-level approval workflows + thresholds

```bash
echo ""
echo "TACHE 6.2.12 : Multi-level approval workflows + thresholds"

# T12-F1 : Table carrier_approval_thresholds
if pg_query "SELECT 1 FROM information_schema.tables WHERE table_name = 'carrier_approval_thresholds'" | grep -q "1"; then
  add_row "T12-F1" "Table carrier_approval_thresholds" "PASS" ""
else
  add_row "T12-F1" "Table carrier_approval_thresholds" "FAIL" "Manquante"
fi

# T12-V1 (P0) : RLS + FORCE
RLS=$(pg_query "SELECT relrowsecurity AND relforcerowsecurity FROM pg_class WHERE relname = 'carrier_approval_thresholds'")
if [ "$RLS" = "t" ]; then
  add_row "T12-V1" "RLS + FORCE" "PASS" ""
else
  pg_query_raw "ALTER TABLE carrier_approval_thresholds ENABLE ROW LEVEL SECURITY; ALTER TABLE carrier_approval_thresholds FORCE ROW LEVEL SECURITY;"
  RLS2=$(pg_query "SELECT relrowsecurity AND relforcerowsecurity FROM pg_class WHERE relname = 'carrier_approval_thresholds'")
  [ "$RLS2" = "t" ] && add_row "T12-V1" "RLS + FORCE" "PASS*" "Repare auto" || add_row "T12-V1" "RLS + FORCE" "FAIL" "Non activable"
fi

# T12-V2 : Service 3 methodes
SERVICE="repo/apps/web-carrier-portal/lib/services/carrier-approval-thresholds.service.ts"
METHODS=(getThresholds setThresholds evaluateRequiredApprovals)
OK=0; for m in "${METHODS[@]}"; do grep -q "$m" "$SERVICE" 2>/dev/null && OK=$((OK + 1)); done
[ "$OK" -eq 3 ] && add_row "T12-V2" "Service 3 methodes" "PASS" "" || add_row "T12-V2" "Service 3 methodes" "FAIL" "$OK / 3"

# T12-V3 : Page admin thresholds (carrier_admin only)
[ -f "repo/apps/web-carrier-portal/app/admin/thresholds/page.tsx" ] && add_row "T12-V3" "Page admin thresholds" "PASS" "" || add_row "T12-V3" "Page admin thresholds" "FAIL" "Manquant"

# T12-V4 : Tests 10+
cd repo
T12_TESTS=$(pnpm vitest run --reporter=json apps/web-carrier-portal/test/approval-thresholds.spec.ts 2>/dev/null | jq '.numPassedTests // 0' 2>/dev/null || echo 0)
cd ..
[ "$T12_TESTS" -ge 10 ] && add_row "T12-V4" "Tests >= 10" "PASS" "$T12_TESTS" || add_row "T12-V4" "Tests >= 10" "FAIL" "$T12_TESTS / 10"
```

---

## TACHE 13/13 -- 6.2.13 : Tests E2E 45+ + seeds 5 carriers fixtures

```bash
echo ""
echo "TACHE 6.2.13 : Tests E2E 45+ + seeds 5 carriers"

# T13-V1 : Tests E2E Playwright >= 45
cd repo
TESTS_E2E=$(pnpm playwright test --reporter=json apps/web-carrier-portal/e2e 2>/dev/null | jq '.suites[].specs | length' 2>/dev/null | awk '{s+=$1} END {print s}' || echo 0)
cd ..
if [ "${TESTS_E2E:-0}" -ge 45 ]; then
  add_row "T13-V1" "Tests E2E >= 45" "PASS" "$TESTS_E2E tests"
elif [ "${TESTS_E2E:-0}" -ge 35 ]; then
  add_row "T13-V1" "Tests E2E >= 45" "WARN" "$TESTS_E2E / 45 (tolere)"
else
  add_row "T13-V1" "Tests E2E >= 45" "FAIL" "$TESTS_E2E / 45"
fi

# T13-V2 : Seeds 5 carriers Maroc
SEEDS="repo/infrastructure/scripts/seed-carriers-fixtures-5.ts"
if [ -f "$SEEDS" ]; then
  COUNT=$(grep -cE "Wafa|RMA|Saham|AXA|Atlanta" "$SEEDS" 2>/dev/null)
  if [ "${COUNT:-0}" -ge 5 ]; then
    add_row "T13-V2" "Seeds 5 carriers Maroc" "PASS" "$COUNT carriers"
  else
    add_row "T13-V2" "Seeds 5 carriers Maroc" "WARN" "$COUNT / 5"
  fi
else
  add_row "T13-V2" "Seeds 5 carriers Maroc" "FAIL" "Seeds manquant"
fi

# T13-V3 : Coverage modules critiques >= 90% (payment-approval + honoraires-approval)
cd repo
CRITICAL_COV=$(pnpm vitest run --coverage --reporter=json apps/web-carrier-portal/lib/services/carrier-payment-approval.service.spec.ts apps/web-carrier-portal/lib/services/carrier-honoraires-approval.service.spec.ts 2>/dev/null | jq '.coverageMap.total.lines.pct // 0' 2>/dev/null || echo 0)
cd ..
if (( $(echo "$CRITICAL_COV >= 90" | bc -l 2>/dev/null || echo 0) )); then
  add_row "T13-V3" "Coverage critiques >= 90%" "PASS" "${CRITICAL_COV}%"
elif (( $(echo "$CRITICAL_COV >= 85" | bc -l 2>/dev/null || echo 0) )); then
  add_row "T13-V3" "Coverage critiques >= 90%" "WARN" "${CRITICAL_COV}%"
else
  add_row "T13-V3" "Coverage critiques >= 90%" "FAIL" "${CRITICAL_COV}%"
fi
```

---

## VERIFICATIONS TRANSVERSALES SPRINT 26.5

```bash
echo ""
echo "TRANSVERSAUX SPRINT 26.5"

cd repo

# TR-BUILD
pnpm turbo run build 2>&1 > /tmp/build.log; BC=$?
[ $BC -eq 0 ] && add_row "TR-BUILD" "Build" "PASS" "" || add_row "TR-BUILD" "Build" "FAIL" "Erreurs"

# TR-TYPECHECK
pnpm tsc --noEmit 2>&1 > /tmp/tsc.log
TS_ERR=$(grep -c "error TS" /tmp/tsc.log)
[ "$TS_ERR" -eq 0 ] && add_row "TR-TYPECHECK" "TypeScript strict" "PASS" "0 erreur" || add_row "TR-TYPECHECK" "TypeScript strict" "FAIL" "$TS_ERR erreurs"

# TR-TESTS baseline + Sprint 26.5
VITEST_OUT=$(pnpm vitest run --coverage --reporter=json 2>/dev/null)
TESTS_PASSED=$(echo "$VITEST_OUT" | jq '.numPassedTests // 0' 2>/dev/null || echo 0)
TESTS_TOTAL=$(echo "$VITEST_OUT" | jq '.numTotalTests // 0' 2>/dev/null || echo 0)
# Baseline cumul attendu = 1356 (post-S24) + 130 (S22.5 75) + 90 (S22.7 35) + 110 (S26.5 45) = 1686+
if [ "$TESTS_TOTAL" -ge 1686 ] && [ "$TESTS_PASSED" -eq "$TESTS_TOTAL" ]; then
  add_row "TR-TESTS" "Tests Vitest >= 1686" "PASS" "$TESTS_PASSED/$TESTS_TOTAL"
else
  add_row "TR-TESTS" "Tests Vitest baseline+sprints" "WARN" "$TESTS_PASSED/$TESTS_TOTAL"
fi

# TR-LINT
pnpm lint 2>&1 > /tmp/lint.log; LC=$?
[ $LC -eq 0 ] && add_row "TR-LINT" "Biome lint" "PASS" "" || add_row "TR-LINT" "Biome lint" "WARN" ""

cd ..

# TR-NO-EMOJI
EMOJI=$(grep -rP "[\x{1F300}-\x{1F9FF}]|[\x{2600}-\x{27BF}]" repo/apps/web-carrier-portal --include="*.ts" --include="*.tsx" --include="*.md" 2>/dev/null | wc -l)
[ "$EMOJI" -eq 0 ] && add_row "TR-NO-EMOJI" "0 emoji" "PASS" "decision-006" || add_row "TR-NO-EMOJI" "0 emoji" "FAIL" "$EMOJI"

# TR-CONSOLE
CONSOLE=$(grep -rn "console\.log\|console\.error" repo/apps/web-carrier-portal --include="*.ts" --include="*.tsx" 2>/dev/null | grep -v ".spec.ts" | grep -v ".test.ts" | wc -l)
[ "$CONSOLE" -eq 0 ] && add_row "TR-CONSOLE" "0 console.*" "PASS" "Pino obligatoire" || add_row "TR-CONSOLE" "0 console.*" "FAIL" "$CONSOLE"

# TR-COMMITS
NON_CONV=$(git log --since="2 weeks ago" --pretty=format:"%s" -- repo/apps/web-carrier-portal | grep -vE "^(feat|fix|docs|test|chore|refactor|perf)(\(.+\))?:" | wc -l)
[ "$NON_CONV" -eq 0 ] && add_row "TR-COMMITS" "Conventional commits" "PASS" "" || add_row "TR-COMMITS" "Conventional commits" "WARN" "$NON_CONV"

# TR-RLS new tables (carrier_approval_thresholds + carrier-specific)
NEW_TABLES=(carrier_approval_thresholds)
RLS_OK=0
for t in "${NEW_TABLES[@]}"; do
  RLS=$(pg_query "SELECT relrowsecurity AND relforcerowsecurity FROM pg_class WHERE relname = '$t'")
  [ "$RLS" = "t" ] && RLS_OK=$((RLS_OK + 1))
done
[ "$RLS_OK" -ge 1 ] && add_row "TR-RLS" "RLS + FORCE nouvelles tables" "PASS" "$RLS_OK/1" || add_row "TR-RLS" "RLS + FORCE nouvelles tables" "FAIL" "$RLS_OK / 1"

# TR-AUDIT-ACAPS : Audit log present sur services critiques
ACAPS_REFS=$(grep -rE "compliance_acaps_audits|acapsAuditService" repo/apps/web-carrier-portal/lib/services 2>/dev/null | wc -l)
if [ "${ACAPS_REFS:-0}" -ge 5 ]; then
  add_row "TR-AUDIT-ACAPS" "Audit ACAPS logs sur services critiques" "PASS" "$ACAPS_REFS refs"
else
  add_row "TR-AUDIT-ACAPS" "Audit ACAPS logs sur services critiques" "FAIL" "$ACAPS_REFS / 5 (compliance loi)"
fi

# TR-DECIMAL : decimal.js sur services financiers
DECIMAL_REFS=$(grep -rE "new Decimal|import.*Decimal" repo/apps/web-carrier-portal/lib/services 2>/dev/null | wc -l)
if [ "${DECIMAL_REFS:-0}" -ge 5 ]; then
  add_row "TR-DECIMAL" "decimal.js precision financiere" "PASS" "$DECIMAL_REFS usages"
else
  add_row "TR-DECIMAL" "decimal.js precision financiere" "FAIL" "$DECIMAL_REFS usages (insuffisant)"
fi

# TR-PERMISSIONS Sprint 7.5a (carrier roles)
PERMS_CARRIER=$(grep -rE "carrier\.(experts|tow|expert_reports|sinistres|compliance|analytics)\." repo/apps/web-carrier-portal 2>/dev/null | wc -l)
if [ "${PERMS_CARRIER:-0}" -ge 10 ]; then
  add_row "TR-PERMS" "Permissions carrier.* enforces" "PASS" "$PERMS_CARRIER usages"
else
  add_row "TR-PERMS" "Permissions carrier.* enforces" "WARN" "$PERMS_CARRIER usages (verifier coverage)"
fi
```

---

## VERIFICATIONS SPECIFIQUES REPLACEMENT MOCKS (3 criteres CRITIQUES)

```bash
echo ""
echo "VERIFICATIONS SPECIFIQUES REPLACEMENT MOCKS"

# REPL-1 (P0 CRITIQUE) : MockCarrierFnolService Sprint 24 -> CarrierFnolReviewService
REAL_FNOL="repo/apps/web-carrier-portal/lib/services/carrier-fnol-review.service.ts"
MOCK_FNOL="repo/packages/repair/src/services/mock-carrier-fnol.service.ts"
if [ -f "$REAL_FNOL" ] && grep -qE "USE_REAL_CARRIER_PORTAL" "$MOCK_FNOL" 2>/dev/null; then
  add_row "REPL-1" "Replacement Mock FNOL Sprint 24 (feature flag)" "PASS" "Flag conditionnel OK"
elif [ -f "$REAL_FNOL" ]; then
  add_row "REPL-1" "Replacement Mock FNOL Sprint 24" "WARN" "Service present mais flag a verifier"
else
  add_row "REPL-1" "Replacement Mock FNOL Sprint 24" "FAIL" "Service real absent"
fi

# REPL-2 (P0 CRITIQUE) : MockCarrierHonorairesApprovalService Sprint 22.5
REAL_HON="repo/apps/web-carrier-portal/lib/services/carrier-honoraires-approval.service.ts"
MOCK_HON="repo/packages/tow/src/services/mock-carrier-honoraires-approval.service.ts"
if [ -f "$REAL_HON" ] && grep -qE "USE_REAL_CARRIER_PORTAL" "$MOCK_HON" 2>/dev/null; then
  add_row "REPL-2" "Replacement Mock Honoraires Sprint 22.5 (feature flag)" "PASS" "Flag conditionnel OK"
elif [ -f "$REAL_HON" ]; then
  add_row "REPL-2" "Replacement Mock Honoraires Sprint 22.5" "WARN" "Service present mais flag a verifier"
else
  add_row "REPL-2" "Replacement Mock Honoraires Sprint 22.5" "FAIL" "Service real absent"
fi

# REPL-3 (P0 CRITIQUE) : MockCarrierPaymentService Sprint 21
REAL_PAY="repo/apps/web-carrier-portal/lib/services/carrier-payment-approval.service.ts"
MOCK_PAY="repo/packages/repair/src/services/carrier-payment-approval.service.ts"
if [ -f "$REAL_PAY" ] && grep -qE "USE_REAL_CARRIER_PORTAL" "$MOCK_PAY" 2>/dev/null; then
  add_row "REPL-3" "Replacement Mock Payment Sprint 21 (feature flag)" "PASS" "Flag conditionnel OK"
elif [ -f "$REAL_PAY" ]; then
  add_row "REPL-3" "Replacement Mock Payment Sprint 21" "WARN" "Service present mais flag a verifier"
else
  add_row "REPL-3" "Replacement Mock Payment Sprint 21" "FAIL" "Service real absent"
fi
```

---

## RAPPORT FINAL CONSOLIDE

```bash
TOTAL=$((PASS + PASS_REPAIRED + FAIL + SKIP + WARN))
SCORE_NUM=$((PASS + PASS_REPAIRED))
SCORE_PCT=$(echo "scale=1; $SCORE_NUM * 100 / $TOTAL" | bc -l 2>/dev/null || echo "0")

JALON="NO-GO"
REPLACEMENT_STATUS="MOCKS ACTIFS"
if (( $(echo "$SCORE_PCT >= 95" | bc -l 2>/dev/null || echo 0) )); then
  JALON="GO"
  REPLACEMENT_STATUS="MOCKS REMPLACES (USE_REAL_CARRIER_PORTAL=true)"
elif (( $(echo "$SCORE_PCT >= 85" | bc -l 2>/dev/null || echo 0) )); then
  JALON="GO CONDITIONNEL"
  REPLACEMENT_STATUS="REPLACEMENT PARTIEL (feature flag transition)"
fi

cat >> "$REPORT_FILE" << EOF

## Synthese finale

| Categorie | Nombre |
|-----------|--------|
| PASS | $PASS |
| PASS* (repare) | $PASS_REPAIRED |
| FAIL | $FAIL |
| SKIP | $SKIP |
| WARN | $WARN |
| **TOTAL** | **$TOTAL** |

**Score** : $SCORE_PCT% ($SCORE_NUM / $TOTAL)
**Jalon** : $JALON
**Status replacement mocks** : $REPLACEMENT_STATUS

---

## Resultats detailles

| ID | Description | Statut | Details |
|----|-------------|--------|---------|
$(echo -e "$TABLE_ROWS")

---

## Decision

EOF

case "$JALON" in
  GO)
    cat >> "$REPORT_FILE" << EOF
**GO** -- Sprint 26.5 v3.0 valide. Carrier Portal operationnel.

Actions :
1. \`git tag -a "sprint-26.5-complete-v3-carrier-portal" -m "Sprint 26.5 v3.0 -- score $SCORE_PCT%"\`
2. Activer feature flag production : \`export USE_REAL_CARRIER_PORTAL=true\`
3. Configurer thresholds par carrier (50k MAD default)
4. Lancer Sprint 27 Tenants Management
5. Demo Day 30 juin 2026 = carrier scenarios LIVE (no mock)
EOF
    ;;
  "GO CONDITIONNEL")
    cat >> "$REPORT_FILE" << EOF
**GO CONDITIONNEL** ($SCORE_PCT%) -- Replacement partiel.

Actions :
1. Documenter dette dans \`dette-technique-sprint-26.5.md\`
2. Resoudre FAIL P0 (REPL-1/2/3 priorite max)
3. Demo Day = feature flag mixte (real + mock fallback)
4. Tag : \`git tag -a "sprint-26.5-complete-v3-conditional" -m "Sprint 26.5 v3.0 conditional -- score $SCORE_PCT%"\`
EOF
    ;;
  NO-GO)
    cat >> "$REPORT_FILE" << EOF
**NO-GO** ($SCORE_PCT%) -- Carrier Portal incomplet.

Actions :
1. Escalation Saad + Abla (impact Demo Day)
2. Identifier FAIL P0 (specialement REPL-1/2/3)
3. Re-executer taches FAIL
4. Re-V-26.5 dans 5 jours
5. Demo Day = mocks Sprint 21/22.5/24 conservees (decision-015 fallback)
6. NE PAS lancer Sprint 27 avant GO obtenu
EOF
    ;;
esac

echo ""
echo "================================================"
echo "RAPPORT GENERE : $REPORT_FILE"
echo "Score : $SCORE_PCT% / Jalon : $JALON"
echo "Replacement status : $REPLACEMENT_STATUS"
echo "================================================"

cat "$REPORT_FILE"
```

---

## NOTES IMPORTANTES POUR EXECUTION

1. **Auto-reparation** : RLS + FORCE auto-repair sur carrier_approval_thresholds
2. **Tests baseline cumules** : 1356 (post-Sprint 24) + 130 (S22.5) + 90 (S22.7) + 110 (S26.5) = 1686+ tests
3. **3 verifications REPLACEMENT MOCKS CRITIQUES** (REPL-1 + REPL-2 + REPL-3) = feature flag USE_REAL_CARRIER_PORTAL obligatoire
4. **Audit ACAPS** (TR-AUDIT-ACAPS) = compliance loi non-negociable sur services critiques
5. **decimal.js** (TR-DECIMAL) = precision financiere obligatoire payment + honoraires
6. **Multi-level approval > 50k MAD** (T08-V3) = critique pour eviter fraude
7. **Coverage modules critiques** (T13-V3) >= 90% = payment-approval + honoraires-approval (decisions financieres)
8. **Score GO >= 95%** = Demo Day live carrier scenarios / GO CONDITIONNEL 85-95% = transition mixte / NO-GO < 85% = fallback mocks

---

**Fin verification V-26.5 v3.0 -- Sprint 26.5 (6.2) Carrier Portal.**

**Total criteres** : 70 + 11 transversaux + 3 replacement mocks = 84 criteres
