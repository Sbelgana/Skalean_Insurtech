# VERIFICATION SPRINT 7.5b -- Phase 2.5 / Sprint Migration #2 : Specifications v3.0 Complete
# Auto-checks + criteres GO/NO-GO + jalon avant Sprint 8 demarrage
# AUCUNE EMOJI AUTORISEE

**Version** : v3.0 (Option B Migration -- Sprint 7.5b verification)
**Phase** : 2.5 -- Migration Assurflow
**Sprint** : 7.5b / 40 (cumul v3.0) -- Sprint Migration #2
**Reference orchestrateur** : `C-7.5b-sprint-7.5b-specifications-v3.md`
**Numerotation criteres** : V-7.5b.1 a V-7.5b.12 (1 critere par tache) + V-TR (transversales)
**Effort verification** : ~2 heures (run scripts + review manual)
**Priorite** : P0 ABSOLUE (gate avant Sprint 8 Phase 3 demarrage)

---

Tu es **Claude Code (ou Cowork) executant la verification**. Pour chaque critere ci-dessous :
1. **Execute** la commande/check
2. **Note** le resultat (PASS / FAIL / WARN)
3. **Si FAIL P0** : tente auto-reparation (3 tentatives max) puis escalade
4. **Si FAIL P1/P2** : note pour suite mais continue

A la fin : **rapport final** + **jalon GO / GO CONDITIONNEL / NO-GO**.

---

## REGLES VERIFICATION

### Priorisation
- **P0** = bloquant (FAIL = NO-GO Sprint 8)
- **P1** = important (FAIL = GO CONDITIONNEL + fix Sprint 8)
- **P2** = mineur (FAIL = note + fix opportuniste)

### Format rapport (a generer en fin)
```
Sprint 7.5b Verification Report -- {DATE}
==========================================

Criteres testes : 95
PASS : XX
FAIL : XX (XX P0 / XX P1 / XX P2)
WARN : XX

Jalon : GO / GO CONDITIONNEL / NO-GO

Details FAIL :
- V-7.5b.X (P0) : {description} -> action requise
- V-7.5b.Y (P1) : {description} -> note
```

---

# CRITERES V-7.5b.1 a V-7.5b.12 (1 par tache orchestrateur)

## V-7.5b.1 -- Integration 6 meta-prompts (Tache 7.5b.1)

### V-7.5b.1.1 (P0) : 6 meta-prompts presents dans repo

```bash
# Test
META_PROMPTS_V3=$(ls 00-pilotage/meta-prompts/B-{14,21,24}-* 2>/dev/null | wc -l)
META_PROMPTS_NEW=$(ls 00-pilotage/meta-prompts/B-22.5-* 00-pilotage/meta-prompts/B-22.7-* 00-pilotage/meta-prompts/B-26.5-* 2>/dev/null | wc -l)

echo "Refondus (B-14/21/24): $META_PROMPTS_V3 (expected 3)"
echo "Nouveaux (B-22.5/22.7/26.5): $META_PROMPTS_NEW (expected 3)"

[ "$META_PROMPTS_V3" -eq 3 ] && [ "$META_PROMPTS_NEW" -eq 3 ] && echo "PASS" || echo "FAIL"
```

**Auto-reparation si FAIL** : 
```bash
# Re-executer Tache 7.5b.1 copie
cp /mnt/user-data/outputs/sprint-7.5b-meta-prompts/*.md 00-pilotage/meta-prompts/
# Renommer v3 files
cd 00-pilotage/meta-prompts/
mv B-14-sprint-14-insure-foundation-v3.md B-14-sprint-14-insure-foundation.md 2>/dev/null
mv B-21-sprint-21-sinistre-workflow-v3.md B-21-sprint-21-sinistre-workflow.md 2>/dev/null
mv B-24-sprint-24-flux-sinistre-v3.md B-24-sprint-24-flux-sinistre-client.md 2>/dev/null
```

### V-7.5b.1.2 (P1) : Backup v2.2 cree

```bash
BACKUP_COUNT=$(ls 00-pilotage/meta-prompts/archive-v2.2/B-{14,21,24}-* 2>/dev/null | wc -l)
echo "Backups v2.2: $BACKUP_COUNT (expected 3)"
[ "$BACKUP_COUNT" -eq 3 ] && echo "PASS" || echo "FAIL"
```

### V-7.5b.1.3 (P1) : Meta-prompts v3.0 ont taille attendue (validation contenu non vide)

```bash
B14_LINES=$(wc -l < 00-pilotage/meta-prompts/B-14-sprint-14-insure-foundation.md)
B21_LINES=$(wc -l < 00-pilotage/meta-prompts/B-21-sprint-21-sinistre-workflow.md)
B24_LINES=$(wc -l < 00-pilotage/meta-prompts/B-24-sprint-24-flux-sinistre-client.md)
B225_LINES=$(wc -l < 00-pilotage/meta-prompts/B-22.5-sprint-22.5-tow-app.md)
B227_LINES=$(wc -l < 00-pilotage/meta-prompts/B-22.7-sprint-22.7-expert-app.md)
B265_LINES=$(wc -l < 00-pilotage/meta-prompts/B-26.5-sprint-26.5-carrier-portal.md)

echo "B-14: $B14_LINES (>= 800)"
echo "B-21: $B21_LINES (>= 1500)"
echo "B-24: $B24_LINES (>= 1200)"
echo "B-22.5: $B225_LINES (>= 1200)"
echo "B-22.7: $B227_LINES (>= 1000)"
echo "B-26.5: $B265_LINES (>= 1300)"

[ "$B14_LINES" -ge 800 ] && [ "$B21_LINES" -ge 1500 ] && [ "$B24_LINES" -ge 1200 ] && \
[ "$B225_LINES" -ge 1200 ] && [ "$B227_LINES" -ge 1000 ] && [ "$B265_LINES" -ge 1300 ] && echo "PASS" || echo "FAIL"
```

### V-7.5b.1.4 (P0) : 0 emoji dans meta-prompts v3.0

```bash
EMOJI_COUNT=$(grep -P "[\x{1F300}-\x{1F9FF}]" 00-pilotage/meta-prompts/B-{14,21,22.5,22.7,24,26.5}-*.md 2>/dev/null | wc -l)
echo "Emoji dans meta-prompts v3.0: $EMOJI_COUNT (expected 0)"
[ "$EMOJI_COUNT" -eq 0 ] && echo "PASS" || echo "FAIL"
```

### V-7.5b.1.5 (P1) : Commit conforme conventional commits

```bash
LAST_COMMIT_MSG=$(git log -1 --grep="meta-prompts v3.0" --pretty=format:"%s")
echo "Last commit: $LAST_COMMIT_MSG"
echo "$LAST_COMMIT_MSG" | grep -qE "^docs\(meta-prompts\):" && echo "PASS" || echo "FAIL"
```

---

## V-7.5b.2 -- Plan v3.0 INDEX + README (Tache 7.5b.2)

### V-7.5b.2.1 (P0) : INDEX.md contient sections v3.0 obligatoires

```bash
INDEX="00-pilotage/INDEX.md"

declare -A REQUIRED=(
  ["v3.0"]="version reference"
  ["40 sprints"]="total sprints"
  ["6 acteurs"]="ecosystem"
  ["Phase 2.5"]="phase migration"
  ["B-7.5a"]="meta-prompt foundation"
  ["B-7.5b"]="meta-prompt specifications"
  ["B-22.5"]="Tow"
  ["B-22.7"]="Expert"
  ["B-26.5"]="Carrier"
  ["B-20.5"]="Sky AI"
)

PASS=0
TOTAL=${#REQUIRED[@]}
for key in "${!REQUIRED[@]}"; do
  if grep -q "$key" "$INDEX"; then
    PASS=$((PASS + 1))
  else
    echo "MANQUANT: $key (${REQUIRED[$key]})"
  fi
done

echo "INDEX v3.0 references: $PASS / $TOTAL"
[ "$PASS" -ge $((TOTAL - 1)) ] && echo "PASS" || echo "FAIL"
```

### V-7.5b.2.2 (P0) : README.md mentionne Skalean + Assurflow

```bash
README="00-pilotage/README.md"
grep -q "Skalean" "$README" && grep -q "Assurflow" "$README" && echo "PASS" || echo "FAIL"
```

### V-7.5b.2.3 (P1) : Statistiques v3.0 documentees (comparaison v2.2 vs v3.0)

```bash
grep -q "v2.2" "00-pilotage/INDEX.md" && grep -q "v3.0" "00-pilotage/INDEX.md" && \
grep -q "26 roles" "00-pilotage/INDEX.md" && grep -q "130" "00-pilotage/INDEX.md" && \
echo "PASS" || echo "FAIL"
```

### V-7.5b.2.4 (P1) : Lecture recommandee (ordre) presente

```bash
grep -q "Lecture recommandee" "00-pilotage/README.md" && echo "PASS" || echo "FAIL"
```

---

## V-7.5b.3 -- 8-master-prompt update v3.0 (Tache 7.5b.3)

### V-7.5b.3.1 (P0) : 8-master mentionne 26 roles + 6 acteurs + workflow expert

```bash
MASTER=$(ls 00-pilotage/documentation/8-*-master*.md 2>/dev/null | head -1)
[ -z "$MASTER" ] && echo "FAIL: 8-master not found" && exit 1

grep -q "26 roles" "$MASTER" && grep -q "6 acteurs" "$MASTER" && \
grep -qi "expert.*designe.*carrier" "$MASTER" && echo "PASS" || echo "FAIL"
```

### V-7.5b.3.2 (P1) : WhatsApp scope strict documente

```bash
grep -qi "whatsapp.*status" "$MASTER" && echo "PASS" || echo "FAIL"
```

### V-7.5b.3.3 (P1) : PartsHub commission documentee

```bash
grep -qi "partshub.*commission\|commission.*3.*5\|commission.*4" "$MASTER" && echo "PASS" || echo "FAIL"
```

### V-7.5b.3.4 (P1) : Atlas Cloud Services mentionne

```bash
grep -qi "atlas.*cloud\|data residency\|maroc.*data" "$MASTER" && echo "PASS" || echo "FAIL"
```

---

## V-7.5b.4 -- 9-roadmap update v3.0 (Tache 7.5b.4)

### V-7.5b.4.1 (P0) : 9-roadmap contient 40 sprints

```bash
ROADMAP="00-pilotage/documentation/9-roadmap-execution.md"
grep -q "40 sprints" "$ROADMAP" && echo "PASS" || echo "FAIL"
```

### V-7.5b.4.2 (P0) : Demo Day 30 juin documente

```bash
grep -q "30 juin 2026\|Demo Day" "$ROADMAP" && echo "PASS" || echo "FAIL"
```

### V-7.5b.4.3 (P1) : Pilote Marrakech Mars-Avril 2027 documente

```bash
grep -qi "pilote.*marrakech\|mars.*2027\|avril.*2027" "$ROADMAP" && echo "PASS" || echo "FAIL"
```

### V-7.5b.4.4 (P1) : Fallback plan J-15 documente

```bash
grep -qi "fallback\|j-15\|15 juin" "$ROADMAP" && echo "PASS" || echo "FAIL"
```

---

## V-7.5b.5 -- 10-arborescence update v3.0 (Tache 7.5b.5)

### V-7.5b.5.1 (P0) : 10-arborescence liste 12 apps

```bash
ARBO="00-pilotage/documentation/10-arborescence-projet.md"

# Compter occurrences apps frontend (web-*)
APPS=$(grep -oE "web-(broker|garage|garage-mobile|customer|assure|admin|landing|tow-mobile|carrier-portal|expert|expert-mobile)" "$ARBO" | sort -u | wc -l)
echo "Apps frontend listees: $APPS (expected >= 11)"
[ "$APPS" -ge 11 ] && echo "PASS" || echo "FAIL"
```

### V-7.5b.5.2 (P0) : 3 nouveaux packages mentionnes

```bash
grep -q "@insurtech/tow" "$ARBO" && grep -q "@insurtech/expertise" "$ARBO" && \
grep -q "@insurtech/carrier" "$ARBO" && echo "PASS" || echo "FAIL"
```

### V-7.5b.5.3 (P1) : 22 packages total documente

```bash
grep -qE "22 packages|~22 packages" "$ARBO" && echo "PASS" || echo "FAIL"
```

---

## V-7.5b.6 -- Refontes mineures B-09 + B-17 + B-18 + B-32 (Tache 7.5b.6)

### V-7.5b.6.1 (P1) : B-09 mentionne WhatsApp scope strict

```bash
grep -qi "whatsapp.*status only\|scope strict\|whitelist" 00-pilotage/meta-prompts/B-09-*.md && echo "PASS" || echo "FAIL"
```

### V-7.5b.6.2 (P1) : B-17 mentionne Sky AI + reseau Assurflow

```bash
grep -qi "sky ai\|reseau.*assurflow" 00-pilotage/meta-prompts/B-17-*.md && echo "PASS" || echo "FAIL"
```

### V-7.5b.6.3 (P1) : B-18 mentionne remorqueur + expert tracking

```bash
grep -qi "remorqueur\|tow\|expert.*tracking" 00-pilotage/meta-prompts/B-18-*.md && echo "PASS" || echo "FAIL"
```

### V-7.5b.6.4 (P1) : B-32 mentionne 8 carriers (incluant MATU + Sanad)

```bash
grep -qE "8 carriers|MATU|Sanad" 00-pilotage/meta-prompts/B-32-*.md && echo "PASS" || echo "FAIL"
```

---

## V-7.5b.7 -- Creation B-20.5 Sky AI Pre-Training (Tache 7.5b.7)

### V-7.5b.7.1 (P0) : B-20.5 cree

```bash
[ -f 00-pilotage/meta-prompts/B-20.5-sprint-20.5-sky-ai-pre-training.md ] && echo "PASS" || echo "FAIL"
```

### V-7.5b.7.2 (P0) : B-20.5 mentionne historique garage Saad

```bash
grep -qi "historique.*garage.*saad\|garage saad\|historique reel" 00-pilotage/meta-prompts/B-20.5-*.md 2>/dev/null && echo "PASS" || echo "FAIL"
```

### V-7.5b.7.3 (P1) : B-20.5 mentionne anonymisation RGPD

```bash
grep -qi "anonymisation\|rgpd\|loi 09-08" 00-pilotage/meta-prompts/B-20.5-*.md 2>/dev/null && echo "PASS" || echo "FAIL"
```

### V-7.5b.7.4 (P1) : B-20.5 a 10 taches documentees

```bash
TACHES=$(grep -cE "^### Tache 20.5\.[0-9]+|^## Tache 20.5\.[0-9]+" 00-pilotage/meta-prompts/B-20.5-*.md 2>/dev/null)
echo "Taches B-20.5: $TACHES (expected >= 8)"
[ "$TACHES" -ge 8 ] && echo "PASS" || echo "FAIL"
```

---

## V-7.5b.8 -- Cross-reference decisions + meta-prompts (Tache 7.5b.8)

### V-7.5b.8.1 (P0) : decision-011 referencee par >= 5 meta-prompts

```bash
REFS=$(grep -l "decision-011\|011-assurflow-rebrand" 00-pilotage/meta-prompts/B-*.md 2>/dev/null | wc -l)
echo "Refs decision-011: $REFS (expected >= 5)"
[ "$REFS" -ge 5 ] && echo "PASS" || echo "FAIL"
```

### V-7.5b.8.2 (P0) : decision-012 (6 acteurs) referencee par >= 6 meta-prompts

```bash
REFS=$(grep -l "decision-012\|012-6-acteurs" 00-pilotage/meta-prompts/B-*.md 2>/dev/null | wc -l)
echo "Refs decision-012: $REFS (expected >= 6)"
[ "$REFS" -ge 6 ] && echo "PASS" || echo "FAIL"
```

### V-7.5b.8.3 (P0) : decision-013 (expert central) referencee par >= 5 meta-prompts

```bash
REFS=$(grep -l "decision-013\|013-expert" 00-pilotage/meta-prompts/B-*.md 2>/dev/null | wc -l)
echo "Refs decision-013: $REFS (expected >= 5)"
[ "$REFS" -ge 5 ] && echo "PASS" || echo "FAIL"
```

### V-7.5b.8.4 (P1) : decision-014 (PartsHub) referencee par >= 2 meta-prompts

```bash
REFS=$(grep -l "decision-014\|014-partshub" 00-pilotage/meta-prompts/B-*.md 2>/dev/null | wc -l)
echo "Refs decision-014: $REFS (expected >= 2)"
[ "$REFS" -ge 2 ] && echo "PASS" || echo "FAIL"
```

### V-7.5b.8.5 (P1) : decision-015 (Demo Day) referencee par >= 3 meta-prompts

```bash
REFS=$(grep -l "decision-015\|015-demo-day" 00-pilotage/meta-prompts/B-*.md 2>/dev/null | wc -l)
echo "Refs decision-015: $REFS (expected >= 3)"
[ "$REFS" -ge 3 ] && echo "PASS" || echo "FAIL"
```

---

## V-7.5b.9 -- docker-compose ajouter ports (Tache 7.5b.9)

### V-7.5b.9.1 (P0) : 4 nouveaux ports presents dans docker-compose

```bash
COMPOSE="docker-compose.yml"
PORTS_OK=0
for port in 3007 3008 3009 3010; do
  grep -q "$port:3000\|$port:8080" "$COMPOSE" && PORTS_OK=$((PORTS_OK + 1))
done
echo "Ports nouveaux apps: $PORTS_OK / 4"
[ "$PORTS_OK" -eq 4 ] && echo "PASS" || echo "FAIL"
```

**Auto-reparation si FAIL** : re-executer Tache 7.5b.9 patch docker-compose.

### V-7.5b.9.2 (P0) : docker-compose YAML valide

```bash
docker-compose config > /dev/null 2>&1 && echo "PASS" || echo "FAIL"
```

### V-7.5b.9.3 (P1) : 4 services apps frontend declares

```bash
SERVICES=$(grep -cE "^\s+(web-tow-mobile|web-carrier-portal|web-expert|web-expert-mobile):" "$COMPOSE")
echo "Services apps v3.0: $SERVICES / 4"
[ "$SERVICES" -ge 3 ] && echo "PASS" || echo "FAIL"  # 3 minimum tolere (expert-mobile optionnel)
```

---

## V-7.5b.10 -- 3 nouveaux packages squelette (Tache 7.5b.10)

### V-7.5b.10.1 (P0) : 3 dossiers packages existent

```bash
for pkg in tow expertise carrier; do
  [ -d "repo/packages/$pkg" ] || { echo "FAIL: repo/packages/$pkg missing"; exit 1; }
done
echo "PASS"
```

### V-7.5b.10.2 (P0) : Chaque package a package.json + tsconfig + src/index.ts

```bash
PASS=0
for pkg in tow expertise carrier; do
  [ -f "repo/packages/$pkg/package.json" ] && \
  [ -f "repo/packages/$pkg/tsconfig.json" ] && \
  [ -f "repo/packages/$pkg/src/index.ts" ] && PASS=$((PASS + 1))
done
echo "Packages complets: $PASS / 3"
[ "$PASS" -eq 3 ] && echo "PASS" || echo "FAIL"
```

### V-7.5b.10.3 (P0) : pnpm install resout workspace deps (3 nouveaux)

```bash
cd repo && pnpm install 2>&1 | grep -qE "error|ERR_" && echo "FAIL" || echo "PASS"
```

### V-7.5b.10.4 (P0) : typecheck OK (squelette compile)

```bash
cd repo && pnpm typecheck 2>&1 | grep -qE "error TS" && echo "FAIL" || echo "PASS"
```

### V-7.5b.10.5 (P1) : Package name conforme @insurtech/* (decision phase 1)

```bash
for pkg in tow expertise carrier; do
  NAME=$(jq -r '.name' "repo/packages/$pkg/package.json" 2>/dev/null)
  echo "Package $pkg name: $NAME"
  [ "$NAME" = "@insurtech/$pkg" ] || { echo "FAIL: $NAME != @insurtech/$pkg"; exit 1; }
done
echo "PASS"
```

---

## V-7.5b.11 -- Commit + tag (Tache 7.5b.11)

### V-7.5b.11.1 (P0) : Tag `sprint-7.5b-complete-v3-specifications` pose

```bash
git tag -l | grep -q "sprint-7.5b-complete-v3-specifications" && echo "PASS" || echo "FAIL"
```

### V-7.5b.11.2 (P0) : Tag annotated (pas lightweight)

```bash
git cat-file -t sprint-7.5b-complete-v3-specifications 2>/dev/null | grep -q "^tag$" && echo "PASS" || echo "FAIL"
```

### V-7.5b.11.3 (P0) : Working tree clean

```bash
git diff --quiet && git diff --cached --quiet && echo "PASS" || echo "FAIL (untracked or modified)"
```

### V-7.5b.11.4 (P1) : >= 11 commits Sprint 7.5b dans historique

```bash
COMMITS=$(git log --oneline --since="2026-05-23" --until="2026-05-31" --grep="Task: 7.5b" 2>/dev/null | wc -l)
echo "Commits 7.5b: $COMMITS (expected >= 11)"
[ "$COMMITS" -ge 11 ] && echo "PASS" || echo "FAIL"
```

### V-7.5b.11.5 (P1) : CHECKLIST-MASTER-EXECUTION.md section 7.5b cochee

```bash
grep -A 30 "Sprint 7.5b" /mnt/user-data/outputs/CHECKLIST-MASTER-EXECUTION.md 2>/dev/null | \
  grep -c "\[x\]" | xargs -I {} test {} -ge 10 && echo "PASS" || echo "WARN"
```

---

## V-7.5b.12 -- Cowork execution handoff (Tache 7.5b.12)

### V-7.5b.12.1 (P0) : COWORK-EXECUTION-GUIDE-V3.md existe

```bash
[ -f "00-pilotage/COWORK-EXECUTION-GUIDE-V3.md" ] && echo "PASS" || echo "FAIL"
```

### V-7.5b.12.2 (P0) : Guide liste 33 sprints restants

```bash
GUIDE="00-pilotage/COWORK-EXECUTION-GUIDE-V3.md"
SPRINTS=$(grep -cE "Sprint [0-9]+|Sprint [0-9]+\.[0-9]+" "$GUIDE" 2>/dev/null)
echo "Sprints mentionnes: $SPRINTS (expected >= 30)"
[ "$SPRINTS" -ge 30 ] && echo "PASS" || echo "FAIL"
```

### V-7.5b.12.3 (P0) : Pattern prompt Cowork documente

```bash
grep -qi "Tu es Cowork\|Lancer Cowork\|Comment lancer" "$GUIDE" && echo "PASS" || echo "FAIL"
```

### V-7.5b.12.4 (P1) : Sequence sprints critiques Demo Day documente

```bash
grep -qi "Demo Day\|sprints critiques\|cible realiste" "$GUIDE" && echo "PASS" || echo "FAIL"
```

### V-7.5b.12.5 (P1) : Fallback FAIL documente

```bash
grep -qi "verification FAIL\|si FAIL\|reparation" "$GUIDE" && echo "PASS" || echo "FAIL"
```

---

# CRITERES TRANSVERSAUX V-TR

## V-TR.1 (P0) -- Build OK

```bash
cd repo && pnpm typecheck 2>&1 > /tmp/typecheck.log
ERRORS=$(grep -cE "error TS" /tmp/typecheck.log)
echo "Erreurs typecheck: $ERRORS (expected 0)"
[ "$ERRORS" -eq 0 ] && echo "PASS" || echo "FAIL"
```

```bash
cd repo && pnpm build 2>&1 > /tmp/build.log
ERRORS=$(grep -cE "error|ERROR" /tmp/build.log)
echo "Erreurs build: $ERRORS (expected 0)"
[ "$ERRORS" -eq 0 ] && echo "PASS" || echo "FAIL"
```

```bash
cd repo && pnpm lint 2>&1 > /tmp/lint.log
ERRORS=$(grep -cE "error" /tmp/lint.log)
echo "Erreurs lint: $ERRORS (expected 0)"
[ "$ERRORS" -eq 0 ] && echo "PASS" || echo "FAIL"
```

## V-TR.2 (P0) -- Tests no regression

```bash
cd repo && pnpm test 2>&1 | tail -20 > /tmp/test-summary.log
TOTAL=$(grep -oE "Tests:[[:space:]]+[0-9]+" /tmp/test-summary.log | grep -oE "[0-9]+" | head -1)
PASS=$(grep -oE "[0-9]+ passed" /tmp/test-summary.log | grep -oE "[0-9]+" | head -1)
FAIL=$(grep -oE "[0-9]+ failed" /tmp/test-summary.log | grep -oE "[0-9]+" | head -1)

echo "Tests total: $TOTAL"
echo "Tests PASS: $PASS"
echo "Tests FAIL: ${FAIL:-0}"

# >= 1071 tests (Sprint 6 baseline) + 0 regression
[ "$TOTAL" -ge 1071 ] && [ -z "$FAIL" ] || [ "$FAIL" -eq 0 ] && echo "PASS" || echo "FAIL"
```

## V-TR.3 (P0) -- 0 emoji

```bash
EMOJI_COUNT=$(grep -rP "[\x{1F300}-\x{1F9FF}]" --include="*.md" --include="*.ts" --include="*.tsx" --include="*.json" --include="*.yml" 00-pilotage repo 2>/dev/null | wc -l)
echo "Emoji count: $EMOJI_COUNT (expected 0)"
[ "$EMOJI_COUNT" -eq 0 ] && echo "PASS" || echo "FAIL"
```

## V-TR.4 (P0) -- Docker stack UP

```bash
cd repo && docker-compose ps 2>&1 | tail -10 > /tmp/docker-status.log
UNHEALTHY=$(grep -cE "unhealthy|Exit|restarting" /tmp/docker-status.log)
echo "Containers unhealthy: $UNHEALTHY (expected 0)"
[ "$UNHEALTHY" -eq 0 ] && echo "PASS" || echo "FAIL"
```

## V-TR.5 (P1) -- 3 nouveaux packages dans pnpm workspace

```bash
PACKAGES=$(pnpm -r --filter "@insurtech/tow" --filter "@insurtech/expertise" --filter "@insurtech/carrier" exec echo OK 2>/dev/null | grep -c OK)
echo "Packages workspace resolus: $PACKAGES / 3"
[ "$PACKAGES" -eq 3 ] && echo "PASS" || echo "FAIL"
```

## V-TR.6 (P1) -- Git tag pose

```bash
git tag -l | grep -q "sprint-7.5b-complete-v3-specifications" && echo "PASS" || echo "FAIL"
```

---

# RAPPORT FINAL

## Synthese auto-generee

A executer apres tous les criteres :

```bash
#!/bin/bash
# Sprint 7.5b Verification Report Generator

DATE=$(date +"%Y-%m-%d %H:%M:%S")
RESULT_FILE="/tmp/sprint-7.5b-verify-report.md"

cat > "$RESULT_FILE" << 'EOF'
# Sprint 7.5b Verification Report

**Date** : ${DATE}
**Sprint** : 7.5b Specifications v3.0
**Reference** : C-7.5b orchestrateur + V-7.5b verification

## Synthese

| Categorie | PASS | FAIL P0 | FAIL P1 | FAIL P2 | WARN |
|-----------|------|---------|---------|---------|------|
| V-7.5b.1 Meta-prompts | X / 5 | X | X | X | X |
| V-7.5b.2 Plan v3.0 | X / 4 | X | X | X | X |
| V-7.5b.3 8-master | X / 4 | X | X | X | X |
| V-7.5b.4 9-roadmap | X / 4 | X | X | X | X |
| V-7.5b.5 10-arborescence | X / 3 | X | X | X | X |
| V-7.5b.6 Refontes mineures | X / 4 | X | X | X | X |
| V-7.5b.7 B-20.5 | X / 4 | X | X | X | X |
| V-7.5b.8 Cross-references | X / 5 | X | X | X | X |
| V-7.5b.9 Docker-compose | X / 3 | X | X | X | X |
| V-7.5b.10 Packages | X / 5 | X | X | X | X |
| V-7.5b.11 Commit + Tag | X / 5 | X | X | X | X |
| V-7.5b.12 Cowork handoff | X / 5 | X | X | X | X |
| V-TR Transversaux | X / 6 | X | X | X | X |
| **TOTAL** | **X / 57** | **X** | **X** | **X** | **X** |

## Jalon

GO / GO CONDITIONNEL / NO-GO

## Details FAIL P0

(liste FAILs P0 avec action requise)

## Details FAIL P1

(liste FAILs P1 avec note)

## Recommandation suite

Si GO : demarrer Sprint 8 CRM + Booking via Cowork.
Si GO CONDITIONNEL : demarrer Sprint 8 + ticket fix P1 ajoute backlog.
Si NO-GO : reprise tache(s) FAIL puis re-tenter V-7.5b.

EOF

cat "$RESULT_FILE"
```

---

# JALON GO / GO CONDITIONNEL / NO-GO

## GO (peut demarrer Sprint 8 Phase 3 horizontaux)

**Conditions GO** :
- TOUS criteres P0 PASS (zero FAIL P0)
- < 3 FAIL P1
- Tests no regression (>= 1071 tests PASS)
- Build OK + 0 emoji
- Tag pose

**Si GO** : Cowork peut demarrer **Sprint 7 reprise tache 2.3.2** (puisque Sprint 7.5a est livre) puis **Sprint 8** sur architecture v3.0 propre.

## GO CONDITIONNEL

**Conditions GO CONDITIONNEL** :
- 0 FAIL P0
- 3-5 FAIL P1 (mais documentables comme dette technique)
- Tests no regression preserves

**Si GO CONDITIONNEL** : 
- Documenter dette dans `00-pilotage/dette-technique-sprint-7.5b.md`
- Demarrer Sprint 8 + integrer fix dans sprints suivants

## NO-GO

**Conditions NO-GO** :
- >= 1 FAIL P0
- OU >= 5 FAIL P1
- OU regression tests > 5

**Si NO-GO** : 
- Lister les FAILs P0 dans ticket reprise
- Re-executer taches Sprint 7.5b correspondantes
- Re-tenter V-7.5b apres reparations
- NE PAS demarrer Sprint 8 avant GO obtenu

---

# AUTO-REPARATION COMMUNE (si FAIL detecte)

## Cas 1 : Meta-prompts manquants

```bash
# Re-executer Tache 7.5b.1
cp /mnt/user-data/outputs/sprint-7.5b-meta-prompts/*.md 00-pilotage/meta-prompts/
cd 00-pilotage/meta-prompts/
for f in B-*-v3.md; do mv "$f" "${f%-v3.md}.md"; done
```

## Cas 2 : Plan v3.0 INDEX/README incomplet

Re-editer manuellement les fichiers selon Tache 7.5b.2 ou demander a Claude prochaine session.

## Cas 3 : Packages workspace non resolu

```bash
cd repo
rm -rf node_modules pnpm-lock.yaml
pnpm install
pnpm typecheck
```

## Cas 4 : Tests regression

```bash
cd repo
pnpm test 2>&1 | grep -A 5 "FAIL"  # identifier
# Analyser cause + fix code + re-run
```

## Cas 5 : Emoji dans fichiers

```bash
# Scanner emojis
grep -rP "[\x{1F300}-\x{1F9FF}]" --include="*.md" 00-pilotage repo

# Remplacer manuellement par equivalent texte
# Ou supprimer
```

## Cas 6 : Tag manquant

```bash
# Re-executer Tache 7.5b.11
git tag -a "sprint-7.5b-complete-v3-specifications" -m "Sprint 7.5b complete"
git push origin sprint-7.5b-complete-v3-specifications
```

---

# REFERENCES

- C-7.5b-sprint-7.5b-specifications-v3.md (orchestrateur 12 taches)
- B-7.5a / B-7.5b / B-14 v3.0 / B-21 v3.0 / B-22.5 / B-22.7 / B-24 v3.0 / B-26.5 (meta-prompts integres)
- decisions 011-015 (cross-references attendues)
- CHECKLIST-MASTER-EXECUTION.md (section 7.5b)

---

**Fin de la verification V-7.5b Specifications v3.0.**

**Si GO** : demarrer **Sprint 7 reprise tache 2.3.2** puis **Sprint 8 CRM + Booking** via Cowork.
**Si NO-GO** : reparer FAILs P0 + re-tenter V-7.5b.
